# v0.3.2_34 — Publish only exact-replayed complete profiles

## Purpose

Replace catalog fragments as the user-facing Strategy Lab result with complete, self-contained Traffic Strategy profiles that are proven before publication.

## Changes

- add a dedicated profile builder and validator;
- include `--filter-tcp=443`, `--filter-l7=tls`, one static selector, `--out-range=-d10`, and the exact tested desynchronization fragment;
- use `--hostlist-domains=<domain>` for domain targets and `--ipset-ip=<address>` for IPv4 targets;
- reject runtime-global arguments, dynamic temporary list paths, project placeholders, and nested profile separators;
- add an exact-profile runtime adapter that changes only the static selector into the isolated temporary runtime selector;
- replay every displayed profile three times with sequential fresh candidate runtimes;
- retain a shortlist item only when all three results have `all_pass=true` and `profile_exact=true`;
- record target, target type, protocol, port, resolved addresses, profile text, replay results, pass count, and verification state;
- preserve the original catalog fragment in `.strategy` as internal evidence and for current circular-test compatibility;
- wire the focused profile-output contract test into mandatory CI.

## Verification

`test-strategy-lab-profile-output.sh` verifies profile construction, exclusion of runtime-only arguments, exact 3-of-3 replay gating, structured shortlist evidence, and static-selector translation in the isolated runtime adapter.

Patch `_35` must not begin until every required GitHub check for `_34` completes successfully and the remote commit is verified.
