# v0.3.2_37 — Isolate circular validation sessions

## Purpose

Close the evidence-mutation half of hardening finding 11 by separating circular validation
from the completed Strategy Lab job that supplied its candidate set.

## Changes

- add independent circular session directories and opaque session identifiers;
- snapshot parent status, shortlist, and endpoints before any runtime mutation;
- keep active/latest session pointers outside parent jobs;
- move circular state, PID, stop control, log, runtime, DNS artifacts, lifecycle snapshot,
  and restoration evidence into the session;
- preserve lifecycle evidence when circular state changes;
- retain the existing GUI/API parent-job contract;
- provide transition-only `state.json` and `stop` symlink aliases that resolve into the
  active private session and never duplicate or redirect state into the parent job;
- schedule removal of those obsolete compatibility aliases in revision 43;
- leave independent launch serialization and stale-owner restoration to revision 38;
- extend mandatory circular and end-to-end tests with parent-file immutability,
  session-only runtime, and alias-resolution assertions.

## Verification

- shell syntax validation;
- `sh scripts/test-strategy-lab-circular.sh`;
- mandatory domain-diagnostics and end-to-end contracts;
- full CI and FreeBSD package build.
