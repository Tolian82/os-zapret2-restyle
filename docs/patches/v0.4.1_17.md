# v0.4.1_17 — Strategy Lab / Diagnostics RU-EN presentation cleanup

**Date:** 2026-08-15
**Status:** PUBLISHED TESTING PACKAGE · AUTOMATED/PUBLICATION ACCEPTANCE PASS · OWNER-LIVE RU/EN/PERSISTENCE PENDING

## Owner-selected scope

This package implements the visible Strategy Lab / Diagnostics cleanup selected after `_16` owner-live QUIC/UDP verification.

### Circular idle

Ordinary Temporary circular validation output no longer exposes raw JSON such as:

```text
{
  "state": "idle"
}
```

It renders localized ordinary text instead:

- RU: `Состояние: ОЖИДАНИЕ`
- EN: `State: IDLE`

Raw Strategy Lab machine JSON remains available only under the explicitly advanced full-output area.

### Deterministic RU/EN labels

`diagnostics.volt` explicitly follows the selected OPNsense page language for the owner-selected strings:

- `Test Domain Connectivity` / `Тестирование соединения с доменом`
- `Enter a domain and click Test to check HTTPS connectivity.` / `Введите домен и нажмите «Проверка», чтобы проверить HTTPS-соединение.`
- `Blocked Domain / IP` / `Заблокированный домен / IP`
- `Run` / `Запуск`
- `Enable QUIC` / `Включить QUIC`
- `Family` / `Семейство`
- `Endpoints` / `Назначения`
- `Outcome` / `Результат`
- `Restoration` / `Восстановление`
- `Replay` / `Ответы`
- `Complete Traffic Strategy profile` / `Полный профиль Стратегий Трафика`
- `Full output (advanced)` / `Полный вывод (расширенный)`

The implementation overrides incomplete gettext coverage at this page boundary without changing machine protocol/state values.

## Enable QUIC persistence

The persistence mechanism was already implemented in `_14` and is intentionally preserved rather than duplicated:

- model field `strategylab.enablequic` default `0`;
- settings endpoint reads/saves `0/1` under the OPNsense config lock;
- page load reads the saved preference;
- checkbox changes save immediately;
- Strategy Lab launch still snapshots the current value job-locally.

The focused localization test also guards this persistence source contract. Owner-live reload/revisit proof remains pending after `_17` installation.

## Package identity and publication

- `VERSION=0.4.1`
- `PLUGIN_REVISION=17`
- source PR `#250` latest verified head: `9bae088ddd0d26a131cac3fb54b17fa761963c75`
- candidate-defining source merge/tag target: `ebf071122b2613c4fe56b5af4e5e9f07c99e9122`
- testing tag: `v0.4.1_17`
- package asset: `os-zapret2-restyle-0.4.1_17.pkg`
- SHA-256: `92d7d3320246380bef53c7d37364895315e12d55b958c8a5fd657ba9ab213dbf`
- required ABI: `FreeBSD:15:amd64`
- publication workflow run: `31887296681`
- stable Pages/pkg repository promoted: **no**

Machine publication evidence: `docs/verification/evidence/testing-publications/v0.4.1_17.md`.

## Automated/source/package acceptance — PASS

Before source merge:

1. diagnostics guidance/i18n/circular/persistence focused contract PASS;
2. complete project and Strategy Lab corrective matrix PASS;
3. FreeBSD-15 package build/inspection qualification PASS;
4. exact latest-head source merge PASS.

After source merge:

1. persistent testing prerelease `v0.4.1_17` published from the candidate-defining merge;
2. package manifest/digest/tag target verified;
3. immutable package asset available on GitHub;
4. publication-record reconciliation proceeds in PR `#251` because repository settings again prevented GitHub Actions from creating its Draft PR automatically.

## Remaining owner-live acceptance

1. Russian presentation check;
2. English presentation/no-leakage check;
3. Enable QUIC OFF/default reload/revisit persistence check.
