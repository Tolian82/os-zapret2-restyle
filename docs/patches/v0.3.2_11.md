# v0.3.2_11 — Strategy Lab extended TLS 1.2 and HTTP

Patch 9 implements the TCP portion of stage 80.

- TLS 1.2: IPv4, TCP/443, TLS filter, explicit TLS 1.2, HTTP/1.1 bounded GET.
- HTTP: IPv4, TCP/80, HTTP filter, HTTP-only HTTP/1.1 bounded GET.
- one active Zapret2 candidate at a time;
- protocol-specific temporary firewall and runtime contracts;
- structured tested/working results;
- extended mode only;
- mandatory cleanup and restoration unchanged.

Package candidate: `os-zapret2-restyle-0.3.2_11.pkg`.
No tag, release, release asset, or pkg-repository publication is authorized.
