# v0.4.1_12 — Owner canon / zero-memory recovery checkpoint

Status: **DOCS / GOVERNANCE ONLY**
Date: 2026-08-14
Package metadata: unchanged (`VERSION=0.4.1`, `PLUGIN_REVISION=12`)

## 1. What changes and why

This corrective closes four documentation-memory gaps before any further code work:

1. **Owner canon precedence.** The owner's newest unambiguous/confirmed instruction is the current
   intended project direction and supersedes conflicting older documentation. Stale documents cannot
   veto or reverse a newly confirmed direction. Model C is the concrete current example.
2. **Cumulative canonical principles.** Every new durable development principle must be added to the
   always-read `docs/PROJECT_PRINCIPLES.md` in the first synchronized documentation change instead of
   being left only in chat/decision history.
3. **Zero-memory GitHub checkpoints.** Every substantive GitHub delivery must leave enough repository
   documentation that a future session with total loss of chat/model memory — even years later — can
   determine recent completed work, the latest change and intended effect, exact next step, overall
   plan progress and active rules.
4. **DNS factual correction.** The previous local/container DNS path was slow and flaky, but the owner
   fixed it. DNS is now treated as working; the historical failure is not a current blocker/root cause
   without new reproducible evidence.

Files synchronized by this logical docs-only change:

- `AGENTS.md`;
- `docs/PROJECT_PRINCIPLES.md`;
- `docs/START_HERE.md`;
- `docs/PROJECT_STATE.md`;
- `docs/ROADMAP.md`;
- `docs/INDEX.md`;
- `docs/DEVELOPMENT_GUIDE.md`;
- `docs/GITHUB_PUBLICATION.md`;
- `docs/decisions/DEC-2026-08-14-owner-canon-and-zero-memory-recovery.md`;
- `docs/devlog/2026-08-14-owner-canon-zero-memory-checkpoint.md`;
- this patch record.

Non-goals:

- no runtime/source/test behavior change;
- no package metadata increment;
- no new Model-C experiment;
- no reopening of accepted Lua/BLOB/discovery/lifecycle measurements;
- no package publication.

## 2. Intended effect / acceptance

After this change, a brand-new project session that remembers nothing must be able to read the
mandatory startup path and immediately know:

- which permanent principles are active and that new ones must be added there;
- that a newer owner instruction outranks conflicting old active documentation;
- what was done most recently and where its full chronological record lives;
- that the historical DNS problem has been fixed and is not assumed current;
- that Model C remains the confirmed production direction;
- that `_13` Model-C-only finalization is the exact next packaged source task unless the owner gives a
  newer instruction;
- the complete current and long-term plan with completed/deferred work retained.

Acceptance for the docs-only corrective:

- `VERSION=0.4.1` and `PLUGIN_REVISION=12` unchanged;
- no runtime/source/test file changed;
- documentation current-state/handoff/roadmap/principles are mutually consistent;
- latest relevant patch/devlog/decision are linked from mandatory startup documents;
- repository/governance CI for the latest head passes;
- no package build/release required.

## 3. Exact immediate next step

After this docs/governance change is merged and `main` verified, proceed directly to the already
approved `v0.4.1_13` Model-C-only production finalization **unless the owner gives a newer instruction**.

## 4. Complete further plan

1. finish this docs-only Ready PR, latest-head CI, exact-head squash merge and verify `main`;
2. do not publish a new package for this docs-only change;
3. implement `_13`: make Model C the only normal production Stage-60 runtime and remove silent B/A
   production replay;
4. run focused Model-C-only regression + complete Strategy Lab corrective matrix;
5. qualify FreeBSD 15 package;
6. reconcile zero-memory documentation checkpoint before `_13` publication;
7. Ready PR -> latest-head CI -> exact-head squash merge -> verify `main`;
8. publish deterministic `_13` testing `.pkg` when owner testing/package delivery is requested;
9. run one selected owner-live Model-C-only regression;
10. record evidence and close B -> C transition on PASS;
11. select the next product/Strategy-Lab work by newest owner priority and current risk;
12. retain the owner-assisted regression backlog and deferred performance/product directions in
    `docs/ROADMAP.md` until explicitly completed/superseded/rejected.
