# v0.3.2_13 — Strategy Lab arbitrary UDP branch

Patch 11 adds configured request-response UDP testing.

- explicit UDP port required;
- non-empty request payload file required;
- missing configuration means explicit skip and zero candidates;
- a non-empty response is required for success;
- sequential Zapret2 UDP-fragment candidates;
- target-scoped UDP firewall and isolated dvtws2 runtime;
- structured skipped, working, or not-found result;
- cleanup and exact restoration unchanged.

Package candidate: `os-zapret2-restyle-0.3.2_13.pkg`.
No tag, release, release asset, or pkg-repository publication is authorized.
