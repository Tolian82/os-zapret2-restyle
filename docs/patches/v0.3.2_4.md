# Patch v0.3.2_4

Date: 2026-08-04

Package candidate:
`os-zapret2-restyle-0.3.2_4.pkg`

Project version:
`0.3.2` unchanged

==================================================
TYPE
==================================================

Ordinary package patch. No tag, GitHub Release, release assets, or pkg-repository
publication.

==================================================
OBJECTIVE
==================================================

Add the asynchronous Strategy Lab job framework and dormant Diagnostics GUI shell
without activating network tests, changing Zapret2 runtime, or replacing the current
Blockcheck button path.

==================================================
ADDED
==================================================

- start/status/cancel/result launcher contract;
- detached worker through `daemon(8)`;
- common validation/path helpers;
- atomic status and ordered event-state helpers;
- one-active-job and busy behavior;
- per-job PID, cancel marker, status, event, and log files;
- four configd actions;
- four Diagnostics API actions;
- dormant progress, polling, and Stop test GUI helpers;
- focused mocked asynchronous job contract test.

==================================================
CANCELLATION
==================================================

Cancellation returns a partial job result and marks unfinished stages as skipped with
exact approved text:

- Russian: `SKIPPED — отменено`;
- English: `SKIPPED — canseled`.

==================================================
UNCHANGED
==================================================

- current Blockcheck Run button and synchronous endpoint;
- normal Zapret2 service state;
- dvtws2 and supervisor lifecycle;
- PF/IPFW rules;
- DNS/TLS/QUIC/HTTP/UDP behavior;
- Traffic Strategy settings;
- published release artifacts.

==================================================
VERIFICATION
==================================================

- POSIX shell syntax;
- PHP syntax;
- asynchronous job ID and detached launch mock;
- normal framework-only partial completion;
- second-job busy response;
- exact Russian and English cancellation text;
- skipped unfinished stages;
- active-job cleanup and idle state;
- invalid target rejection;
- configd/API/GUI contracts;
- legacy Blockcheck path preserved;
- full CI and FreeBSD package build required before merge.

==================================================
MANUAL TEST BOUNDARY
==================================================

No owner-assisted OPNsense checks are requested for this patch. They remain deferred
until all Strategy Lab implementation patches are published and processed by GitHub.
