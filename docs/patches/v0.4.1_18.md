# v0.4.1_18 — Strategy Lab RU labels and aligned input column

**Date:** 2026-08-15  
**Status:** SOURCE/AUTOMATED/FREEBSD-15/PUBLICATION ACCEPTED — OWNER-LIVE RU/EN PENDING

## Owner-selected scope

Live Russian-mode verification of published `_17` confirmed most of the deterministic RU presentation but exposed two untranslated labels and one layout issue. `_18` is intentionally narrow and does not change Strategy Lab runtime/search semantics.

## Presentation changes

- `Strategy Lab` -> RU `Лаборатория стратегий`;
- `Generic UDP (optional)` -> RU `UDP порт (опционально)`;
- English mode explicitly retains `Strategy Lab` and `Generic UDP (optional)`;
- the Strategy Lab domain/UDP/QUIC left-side control column uses one shared alignment;
- the value controls are shifted slightly left relative to `_17`;
- the long RU `Заблокированный домен / IP` label is forced to one line using a bounded 190 px label column with a 12 px label font and reduced inter-cell padding;
- the domain input, Generic UDP port input and Enable QUIC checkbox all start from the same aligned value column.

## Regression boundary

The focused diagnostics contract guards:

- RU/EN title and Generic UDP label pairs;
- application of the Strategy Lab title and UDP-row localization at page load;
- the shared label/value column classes and bounded layout contract;
- prior `_17` deterministic translations and circular idle presentation;
- existing Enable QUIC default/load/save persistence source contract.

## Package/publication identity

- `VERSION=0.4.1`
- `PLUGIN_REVISION=18`
- package: `os-zapret2-restyle-0.4.1_18.pkg`
- tag/release: `v0.4.1_18`
- required ABI: `FreeBSD:15:amd64`
- source merge/tag target: `fa1b924a5c1d646f0daec13aff6e7406a534c6a3`
- SHA-256: `1ca82e1405c688a5429e1fd1d68da19906bea613323d8d01090bba85068b34f0`
- publication workflow run: `31889449879`
- stable Pages/pkg repository promoted: **no**

Machine publication evidence: [`../verification/evidence/testing-publications/v0.4.1_18.md`](../verification/evidence/testing-publications/v0.4.1_18.md).

## Completed acceptance

1. focused diagnostics RU/EN/layout/persistence contract PASS;
2. complete project and Strategy Lab corrective matrix PASS;
3. FreeBSD-15 package build/inspection qualification PASS;
4. exact latest-head source merge through PR `#252`;
5. persistent testing prerelease/tag/package publication PASS.

## Remaining owner-live acceptance

1. RU check: `Лаборатория стратегий`, `UDP порт (опционально)`, one-line blocked-domain label and aligned domain/UDP/QUIC controls;
2. EN check and no RU/EN leakage;
3. separate Enable QUIC OFF/default reload/revisit persistence proof.

Owner-live predecessor evidence: [`../verification/evidence/2026-08-15-v0.4.1_17-ru-presentation-owner-live-followup.md`](../verification/evidence/2026-08-15-v0.4.1_17-ru-presentation-owner-live-followup.md).
