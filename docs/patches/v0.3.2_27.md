# v0.3.2_27 — Strategy Lab preflight cleanup

## Purpose

Prevent stale temporary Strategy Lab state from contaminating network prechecks, baseline probes, or candidate evaluation.

## Changes

- run mandatory cleanup before any worker stage begins;
- stop every reserved Strategy Lab candidate runtime and prove its divert socket is released;
- delete all rules in the exclusively reserved IPFW range `19100–19131`;
- require the reserved firewall range and candidate runtime to be empty before baseline collection;
- persist a terminal stage-00 error and clear the active-job marker when cleanup cannot be proven;
- intentionally omit ownership, occupancy, snapshot, and restoration handling for the reserved IPFW range;
- add the focused preflight-cleanup test to the mandatory domain-diagnostics CI suite.

## Verification

`test-strategy-lab-preflight-cleanup.sh` verifies the exact cleanup order and the terminal failure path. The central diagnostics contract invokes this test in required CI.

Patch `_28` must not begin until all required GitHub checks for `_27` complete successfully and the remote commit is verified.
