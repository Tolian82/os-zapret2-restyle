# v0.3.2_43 — Remove obsolete Strategy Lab surfaces

- remove global circular `state.json` and `stop` compatibility symlinks;
- make private session state and `stop.request` the only circular runtime contract;
- remove the unused state-level unfinished-stage hook;
- retain explicit worker-local skip orchestration for cancel, error, prerequisite, and timeout paths;
- modernize the end-to-end circular fixture and require terminal `completed` state;
- add mandatory obsolete-surface regression coverage.
