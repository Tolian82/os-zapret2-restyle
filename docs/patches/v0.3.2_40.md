# v0.3.2_40 — Restore persisted results after reload

- add atomic `latest.job` pointer and migration fallback;
- prefer active job, then latest persisted job for status lookup;
- render terminal completed/error result on Diagnostics load;
- resume polling only for nonterminal state;
- add mandatory pointer, fallback, and GUI reload tests.

Verification: shell checks, focused reload test, full Strategy Lab suite, CI and FreeBSD package build.
