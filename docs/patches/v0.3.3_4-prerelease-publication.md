# v0.3.3_4 — Prerelease publication

## Scope

Publish the CI-verified `0.3.3_4` corrective candidate as a GitHub prerelease asset for repeating Strategy Lab live scenario 1.

Owner authorization was explicitly granted on `2026-08-06` for prerelease `v0.3.3_4` and asset `os-zapret2-restyle-0.3.3_4.pkg`.

## Publication contract

- trigger only from branch `publish/v0.3.3_4`;
- build inside FreeBSD 15.0;
- require package version `0.3.3_4`;
- require ABI `FreeBSD:15:amd64` and architecture `freebsd:15:x86:64`;
- create tag and prerelease `v0.3.3_4`;
- attach only `os-zapret2-restyle-0.3.3_4.pkg`;
- include the semantic process-inspector correction and Diagnostics idle-reload correction already merged into `main`;
- do not publish GitHub Pages or the pkg repository;
- do not classify the candidate as a stable release.
