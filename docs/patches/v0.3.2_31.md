# v0.3.2_31 — Local-only automatic interception

## Purpose

Prevent automated Strategy Lab probes from diverting matching traffic generated by LAN clients while preserving the explicitly client-wide circular validation mode.

## Changes

- change automatic TCP, TLS 1.2/HTTP, QUIC, and configured UDP IPFW rules from `from any` to `from me`;
- keep destination, port, WAN interface, and reserved divert-port constraints unchanged;
- leave circular validation client-wide and bidirectional because it is an explicit owner-triggered LAN verification mode;
- add a focused source contract test to mandatory CI.

## Verification

`test-strategy-lab-local-traffic-rules.sh` verifies that all automatic candidate rule builders use `from me`, rejects `from any` in those modules, and confirms the circular path still contains client-wide outbound and inbound rules.

Patch `_32` must not begin until all required GitHub checks for `_31` complete successfully and the remote commit is verified.
