# v0.4.1_21 — Laboratory native frame and navigation localization

**Date:** 2026-08-15  
**Status:** SOURCE CANDIDATE — VALIDATION / PUBLICATION PENDING

## Scope

Owner-live `_20` screenshots show two UI defects: Laboratory lost the normal OPNsense outer content perimeter, and Russian `Стратегия` / `Лаборатория` revert to English after navigating to Strategy.

Evidence: [`../verification/evidence/2026-08-15-v0.4.1_20-laboratory-frame-menu-owner-live-followup.md`](../verification/evidence/2026-08-15-v0.4.1_20-laboratory-frame-menu-owner-live-followup.md).

## Implementation

- bump `PLUGIN_REVISION` to `21`;
- remove the redundant Laboratory page wrapper and its `.page-content-main` spacing overrides;
- render Laboratory content boxes inside the platform-owned OPNsense page frame;
- preserve the accepted shared 25% Diagnostics form grid and mode-label typography synchronization;
- keep `Menu.xml` canonical names `Strategy` / `Laboratory`;
- apply active-language `Стратегия` / `Лаборатория` versus `Strategy` / `Laboratory` labels on both Laboratory and Strategy pages;
- keep Strategy Lab execution behavior unchanged.

## Regression boundary

The focused UI regression rejects a Laboratory-owned/overridden `.page-content-main`, loss of the 25% form grid, return of fixed 250px/12px workarounds, loss of mode-label typography synchronization, and loss of deterministic RU/EN submenu labels on either plugin page.

## Delivery boundary

Before completion `_21` must pass focused UI regression, applicable full project validation, FreeBSD-15 package qualification, exact-head merge, testing-package publication, and the bounded publication-record reconciliation.

After install, owner-live verification only needs the corrected perimeter and submenu language plus a quick preservation check of the accepted form grid/mode typography. No Strategy Lab execution rerun is required for this UI-only correction.
