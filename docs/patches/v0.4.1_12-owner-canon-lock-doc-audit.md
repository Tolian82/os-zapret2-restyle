# v0.4.1_12 — Canon-lock and active-documentation reconciliation

Status: **DOCS / GOVERNANCE ONLY**
Date: 2026-08-14
Package metadata: unchanged (`VERSION=0.4.1`, `PLUGIN_REVISION=12`)

## What changes and why

This corrective strengthens the previous zero-memory documentation work after a full active/current-
looking document review found stale text that could still contradict owner canon.

The owner canon being locked is explicit:

- **DNS is fixed.** The old slow/flaky DNS condition is historical and cannot be revived without fresh
  direct reproducible evidence;
- **Model C is selected.** A/B/C production-model selection is closed. A/B are reference/test paths;
  current `_12` B/A fallback is transition debt scheduled for removal by `_13`.

Process rules added:

- one unambiguous owner statement/confirmed decision is enough; do not repeatedly reconfirm it;
- `зафиксируй` requires full reconciliation of all active/current authority docs that could conflict;
- stale tests/CI contracts are updated instead of forcing current docs back toward obsolete canon;
- owner-facing project status is clear Russian with technical terms translated/explained;
- routine temporary branch/repository cleanup is part of the task and normally silent.

The documentation audit found concrete stale current/current-looking text in three places:

- `docs/ARCHITECTURE.md` — still described `_31` and said A/B/C were unselected experiments;
- `docs/architecture/STRATEGY_LAB.md` — still delegated runtime coexistence to the old A/B/C experiment
  plan and described outdated implementation state;
- `docs/verification/STRATEGY_LAB_ADAPTIVE_SEARCH_EXPERIMENTS.md` — still marked itself current and
  described Model B as selected production while Model C remained future.

The first two are rewritten as current Model-C architecture. The experiment file is converted into a
**HISTORICAL / COMPLETED** experiment/evidence index; its full pre-archive text remains available in
Git history at `938d01bca0617d4dad6e4715e637ebd2a3cb11f4`.

Useful active Strategy Lab contracts were explicitly preserved while removing stale selection state:
lifecycle, cancellation, stages, statuses, endpoints, probes, QUIC/IPv6, bilingual report,
Model-C attribution/readiness, cleanup/restoration and the current continuation boundary.

## Intended result / acceptance

After this corrective a zero-memory future session must not be able to interpret any active/current-
looking authority as saying that DNS is still a known problem or that Model B/C selection remains open.

Acceptance:

- current principles contain canon-lock, `зафиксируй`, stale-test, Russian communication and repository
  hygiene rules;
- `AGENTS.md` applies them during every startup/delivery;
- active architecture says Model C is selected and `_13` removes transition fallback;
- old A/B/C experiment plan is explicitly historical/completed rather than current;
- current state/handoff/roadmap remain aligned on `_13` as the next packaged source change;
- DNS is recorded as fixed/currently working;
- no runtime/source/package metadata changes;
- repository checks pass for the final docs-only head.

## Immediate next step

After verified merge and repository cleanup, proceed to `v0.4.1_13` Model-C-only production
finalization unless the owner gives a newer instruction.

## Further plan

1. merge this docs-only corrective after successful checks;
2. verify `main` and remove temporary task branch when it contains no useful unique work;
3. implement `_13` Model-C-only normal Stage-60 production path;
4. validate focused Model-C-only failure/cleanup/attribution behavior and the full Strategy Lab matrix;
5. qualify the FreeBSD 15 package;
6. reconcile the zero-memory handoff with actual `_13` implementation/test evidence;
7. publish/test `_13` when requested by the owner;
8. record owner-live evidence and then select the next roadmap item without reopening A/B/C selection.
