# v0.4.1_13 — Model-C-only production Stage 60

Status: **SOURCE IMPLEMENTED / AUTOMATED QUALIFICATION REQUIRED / OWNER-LIVE PENDING**

## Scope

Finalize the already-selected Model C architecture by removing automatic normal-production replay through Model B and cold Model A.

This patch does not reopen model selection and does not change adaptive search semantics.

## Source change

- add `strategy_lab_py/stage60_model_c_production.py` as the normal production Stage-60 owner;
- route packaged `strategy_lab_python.py stage60-parallel ...` through that owner;
- reuse the existing proven Model-C `_bucket_batch` engine and the authoritative Stage-60 planner;
- translate `ModelCInfrastructureError` into `ModelCOnlyInfrastructureError`, a structural `Stage60ParallelError` that is deliberately not `WarmInfrastructureError`;
- therefore prevent the legacy Model-B/cold-Model-A fallback handler from consuming a normal production Model-C infrastructure failure;
- persist `model_c_only=true` and `cold_fallback_available=false` in Stage-60 result evidence when a result file exists;
- keep explicit `model-b|parallel|cold` overrides as reference/benchmark/test tooling only;
- advance `PLUGIN_REVISION 12 -> 13`, keeping `VERSION=0.4.1`.

## Preserved contracts

Unchanged:

- adaptive graph/planner decisions and candidate order;
- CandidateSpec / ResourceInventory identity;
- source-port leasing and exact attribution;
- profile-compatible physical segmentation inside one logical planner batch;
- `_12` readiness proof;
- `eligible-work-v1` parent budgets;
- bounded GET-4K discovery;
- cleanup/cancellation containment;
- Stage-90 semantic restoration;
- accepted Lua/BLOB/discovery/cross-batch-reuse measurement decisions.

## Regression proof

Focused Model-C production regression must prove:

1. normal entry point routes through the Model-C-only owner;
2. the owner failure class is not `WarmInfrastructureError`;
3. injected Model-C readiness/infrastructure failure raises the structural Model-C-only error;
4. no explicit Model B/A reference path is called by that failure;
5. profile segmentation, source-port dispatch, attribution and cleanup contracts remain intact.

The full Strategy Lab corrective matrix and FreeBSD-15 package qualification are mandatory on the exact PR head before merge.

## Owner-live gate

After a persistent `_13` testing package is deliberately published for testing, run one selected normal Model-C-only OPNsense regression and verify:

- correct result handling;
- no automatic Model B/A replay;
- Stage-90 restoration PASS;
- no temporary IPFW/process/socket residue.

Testing-package publication is separate from source merge and is not implied by this patch.
