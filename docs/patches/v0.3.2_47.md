# v0.3.2_47 — Enforce FreeBSD 15 package CI

## Purpose

Correct the package candidate used for final live OPNsense verification. The revision 46 source closure is valid, but its pull-request package artifact was built on FreeBSD 14.2 and contains ABI `FreeBSD:14:amd64`, while the project target is OPNsense 26.7 / FreeBSD 15 amd64.

## Changes

- build pull-request package candidates only inside FreeBSD 15.0;
- verify the build VM reports FreeBSD major version 15;
- inspect `+MANIFEST` and require `FreeBSD:15:amd64` and `freebsd:15:x86:64`;
- remove truncation-pipe warnings from package inspection;
- add a permanent CI contract rejecting FreeBSD 14 workflow builders;
- mark revision 46 package artifact invalid for installation and live verification;
- select `os-zapret2-restyle-0.3.2_47.pkg` for the owner-assisted live matrix;
- preserve the completed Strategy Lab source/runtime contracts unchanged.

## Verification

- complete Strategy Lab corrective matrix;
- FreeBSD 15 package-workflow contract;
- FreeBSD 15.0 package build;
- generated package ABI and architecture inspection;
- project governance and repository hygiene;
- post-merge `main` integrity.

No tag, GitHub Release, release asset, or pkg-repository publication is included.
