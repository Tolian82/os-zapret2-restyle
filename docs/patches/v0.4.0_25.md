# v0.4.0_25 — Stage-60 controlled source-port lease

==================================================
STATUS
==================================================

**Published testing prerelease; owner-live OPNsense verification pending.**

Published runtime commit:
`a5ecfbfd57820e30e5f2be450e510b96c00267e3`.

Published package:
`os-zapret2-restyle-0.4.0_25.pkg`, size `178783` bytes, digest
`sha256:1355b5000c0a9acc7e6717ddc7bd78248626abd21d826dd82cb8f15fcc4fdf91`.

Publication evidence:
`docs/verification/evidence/2026-08-11-v0.4.0_25-publication.md`.

This patch changes **Stage 60 source-port ownership only**. Search semantics, candidate
identity, endpoint sequencing, width-three candidate overlap, Model-C dispatch, Model-B
fallback semantics, downstream Stage 70/80/85 ownership and Stage-90 restoration remain
unchanged.

==================================================
OWNER-LIVE INPUT FROM `_23`
==================================================

The published `_23` package established both sides of the defect boundary.

Extended `rutracker.org`, `job.FaLtIk`:

- production Stage 60 ran as `C-warm-bucket-source-port-dispatch`;
- all 16 candidates completed;
- winners were `seqovl-host` and `seqovl-midsld`;
- Model C used one physical warm worker and exact source-port-qualified routes;
- no Model-B/Model-A fallback was required;
- Stage 70/80/85 and semantic Stage-90 restoration completed successfully.

Extended `telegram.org`, `job.G0wC5l`:

- IPv4 was available while IPv6 and QUIC were excluded, so this was not a maximal
  protocol/address-family workload;
- Model C encountered `controlled source port is already in use: 42004`;
- the same static concrete source-port plan was then passed to Model B, which failed on the
  same occupied `42004`;
- Stage 60 therefore disabled warm execution and continued through cold Model A;
- 13 candidates completed before `insufficient_stage_budget` / Stage-60 `TIMEOUT`;
- lifecycle restoration was nevertheless semantically exact: RUNNING -> RUNNING, identical
  runtime/config/firewall hashes and `temporary_runtime_clean=true`.

The defect is therefore not the historical `_7`/`_8` parent-timeout problem and not a
restoration failure. The corrective target is the shared static concrete source-port plan.

==================================================
CORRECTION
==================================================

`_25` retains the deterministic `42000+` plan as a preferred identity plan, but concrete
ports are leased independently immediately before each admitted warm batch.

For every candidate/endpoint flow:

1. keep its deterministic preferred source port when the host proves that port free;
2. if it is already in use, leave the foreign socket/process completely untouched;
3. select a unique free alternate above the deterministic plan using the existing audited
   `source-port-free` adapter boundary;
4. render and probe using that exact leased port, preserving the existing source-port-
   qualified IPFW rule, exact curl `--local-port`, exact pinned `--resolve`, counter-growth
   and cleanup attribution contract;
5. retain the endpoint-probe availability recheck as race protection;
6. if Model C becomes unavailable and Model B is entered, Model B performs a **fresh lease**
   rather than inheriting Model C's failed concrete port;
7. if the bounded alternate search cannot prove a free port, fail closed into the existing
   fallback chain rather than weakening attribution or terminating a foreign owner.

Lease evidence records preferred ports, actual leased ports, replacements and the explicit
foreign-port policy `skip-only`.

No Stage-60 timeout is increased in this patch. The source-port defect is corrected first so
that a larger timeout cannot hide an avoidable Model-C -> Model-B -> Model-A fallback.

==================================================
FOLLOW-UP — ADAPTIVE BUDGET
==================================================

A separate follow-up must replace one-size-fits-all Strategy Lab timing with an **adaptive
budget derived from the actual eligible work matrix**:

`number of endpoints × IPv4/IPv6 × TLS/QUIC × Generic UDP × Standard/Extended mode`.

When IPv6, QUIC or Generic UDP are actually available/selected, the job must automatically
receive a reasonable additional budget proportional to the work it can perform. The design
must not be implemented by guessing one oversized global timeout. Budget admission and
stage/overall deadlines must remain finite, observable and cancelable.

This is intentionally documented now but **not implemented by `_25`**. `_25` first removes
the confirmed source-port collision amplification so later timing measurements represent
real Model-C work rather than accidental cold fallback.

==================================================
AUTOMATED VERIFICATION
==================================================

Focused `_25` coverage verifies:

- a free preferred source port is retained;
- an occupied preferred port is skipped without any destructive action;
- a unique alternate is selected and recorded;
- a later Model-B fallback performs a fresh lease and can move again if the previous
  alternate becomes occupied;
- bounded lease exhaustion fails closed;
- the lease wrapper is installed only around the Stage-60 invocation and restores the
  original Model-C/Model-B callables afterward;
- existing Model-C exact-dispatch, Model-B fallback, attribution, cancellation, cleanup,
  restoration and corrective-matrix contracts remain required.

The source/CI gate passed on PR #179 exact head
`2c38ac31ed81dcdf84164ce8d8041385ddad9096`; CI run `31533666585` completed the full
corrective matrix and FreeBSD 15 package build successfully.

==================================================
OWNER-LIVE GATE AFTER PUBLICATION
==================================================

Re-run Extended `telegram.org` from the published `_25` package.

Expected evidence:

- Stage 60 remains `C-warm-bucket-source-port-dispatch` unless a different genuine
  infrastructure failure occurs;
- an occupied preferred `420xx` port, if present, appears as a lease replacement rather
  than forcing both Model C and Model B onto the same collision;
- Model C selector source-port evidence uses the actual leased port;
- Stage 60 completes the available graph or stops for a truthful search/budget reason;
- Stage 90 semantically restores the original Zapret2 state and rules `19128-19130` leave no
  residue.

After this corrective is measured, the documented adaptive-budget follow-up can be designed
from real IPv4/IPv6/TLS/QUIC/UDP workload evidence.
