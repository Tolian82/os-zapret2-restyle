# v0.4.1_19 — Laboratory layout, mode/status localization, and navigation naming

**Date:** 2026-08-15  
**Status:** SOURCE CANDIDATE — AUTOMATED/PUBLICATION ACCEPTANCE PENDING

## Owner-selected scope

Published `_18` fixed the two untranslated Laboratory labels and kept the blocked-domain label on one line, but the live screenshot exposed an unacceptable 12 px typography workaround and field alignment. The owner also selected completion of mode, idle-status, and Zapret navigation naming in the same narrow presentation patch.

## Presentation/layout changes

- remove `_18` `font-size:12px` from the blocked-domain label and restore normal OPNsense UI typography;
- retain one-line `Заблокированный домен / IP` via an explicit fixed label column;
- domain input, Generic UDP input and Enable QUIC checkbox use the same shared value column;
- Strategy Lab input table uses a fixed column layout so row-specific content does not move the left-side controls independently;
- add a right-aligned mode label immediately before the selector: RU `Режим:`, EN `Mode:`;
- RU mode values are `Стандартный` / `Расширенный`;
- EN mode values remain `Standard` / `Extended`;
- ordinary idle presentation is RU `ожидание`, EN `idle`;
- menu canonical child names become `Strategy` / `Laboratory`;
- the active Laboratory page deterministically presents RU `Стратегия` / `Лаборатория` and EN `Strategy` / `Laboratory`.

## Regression boundary

The focused diagnostics/Laboratory contract guards:

- RU/EN Strategy Lab title, Generic UDP, mode-label, mode-value, idle, and navigation pairs;
- absence of the rejected 12 px label workaround;
- fixed label/value columns and common domain/UDP/QUIC alignment contract;
- right-aligned mode-label/selector group;
- menu canonical `Strategy` / `Laboratory` names;
- prior circular ordinary-state presentation;
- existing Enable QUIC default/load/save persistence source contract.

No Strategy Lab candidate search, protocol execution, Model C, lifecycle, Generic UDP, or QUIC execution semantics are changed.

## Plan reconciliation in this patch

The owner explicitly marks these prior backlog rows complete:

- already-accessible target;
- Settings Apply validation/guards;
- Settings Apply service-state correctness.

The next new plan is recorded as:

- add Laboratory support for testing IP addresses as well as domains.

## Package identity

- `VERSION=0.4.1`
- `PLUGIN_REVISION=19`
- target testing asset: `os-zapret2-restyle-0.4.1_19.pkg`
- required ABI: `FreeBSD:15:amd64`

## Required acceptance

Before source merge:

1. focused Laboratory RU/EN/layout/persistence contract PASS;
2. complete project and Strategy Lab corrective matrix PASS;
3. FreeBSD-15 package build/inspection qualification PASS;
4. exact latest-head merge only.

After source merge:

1. persistent testing prerelease `v0.4.1_19` with verified package asset;
2. publication-record reconciliation;
3. owner-live RU typography/alignment/mode/status/sidebar check;
4. owner-live EN mode/status/sidebar and no RU/EN leakage;
5. separate Enable QUIC OFF/default reload/revisit persistence proof remains pending.

Owner-live predecessor evidence: [`../verification/evidence/2026-08-15-v0.4.1_18-laboratory-ui-owner-live-followup.md`](../verification/evidence/2026-08-15-v0.4.1_18-laboratory-ui-owner-live-followup.md).
