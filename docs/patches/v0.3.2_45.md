# v0.3.2_45 — Enforce final corrective CI matrix

- make one script the authoritative Strategy Lab CI entry point;
- run the focused domain contract once and every discovered Strategy Lab test exactly once;
- remove recursive Strategy Lab orchestration from the domain test;
- add a matrix self-contract for workflow, discovery, and nonrecursion;
- eliminate missing UDP-cleanup warnings from terminal and stale-worker fixtures;
- keep non-Strategy project checks and FreeBSD package build as separate CI gates.
