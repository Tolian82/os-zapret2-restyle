# v0.3.3_1 — Live verification prerelease candidate

## Scope

Create a new package identity for owner-assisted live OPNsense verification without changing Strategy Lab runtime behavior.

## Changes

- set `VERSION` to `0.3.3`;
- reset `PLUGIN_REVISION` to `1`;
- build only on FreeBSD 15 amd64;
- make the Strategy Lab activation contract version-aware so revision reset on a semantic version bump remains valid;
- add a dedicated one-shot GitHub Actions publisher for tag and prerelease `v0.3.3_1`;
- publish only `os-zapret2-restyle-0.3.3_1.pkg` as a GitHub prerelease asset;
- do not publish or update the pkg repository;
- keep the stable release gate blocked until the live OPNsense matrix passes.

## Verification

- full project CI;
- Strategy Lab corrective matrix;
- FreeBSD 15 package build;
- package manifest ABI and architecture inspection;
- post-merge `main` integrity;
- prerelease asset availability through the permanent GitHub release URL.

No runtime code behavior changes are included.
