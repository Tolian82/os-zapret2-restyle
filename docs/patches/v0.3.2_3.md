# Patch v0.3.2_3

Date: 2026-08-04

Package candidate:
`os-zapret2-restyle-0.3.2_3.pkg`

Project version:
`0.3.2` unchanged

==================================================
TYPE
==================================================

This is an ordinary package patch. It is not project release `v0.3.3` and does not
create a tag, GitHub Release, release assets, or pkg-repository publication.

==================================================
CHANGED
==================================================

- Replaced the initial English-only Blockcheck guidance on Diagnostics with the exact
  approved English and Russian texts.
- Selected Russian when the OPNsense document language begins with `ru`; English remains
  the default for all other languages.
- Kept the English guidance as the no-JavaScript fallback.
- Rendered the two paragraphs through jQuery `.text()` so translated copy is never
  interpreted as HTML.
- Added focused localization contract coverage to CI.
- Advanced `PLUGIN_REVISION` from `2` to `3` while keeping `VERSION=0.3.2`.

==================================================
VERIFICATION
==================================================

- Confirmed that both approved English paragraphs are present.
- Confirmed that both approved Russian paragraphs are present.
- Confirmed that the obsolete guidance is absent.
- Confirmed POSIX shell syntax for the focused test.
- Confirmed the CI workflow remains valid YAML.

==================================================
UNCHANGED
==================================================

Blockcheck execution, result processing, service lifecycle, settings data, strategy
normalization, runtime behavior, and published v0.3.2 release artifacts are unchanged.
