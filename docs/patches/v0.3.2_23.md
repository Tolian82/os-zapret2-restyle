# Patch candidate `v0.3.2_23` — Explicit domain target contract

## Scope

Corrective Patch 9 aligns the API, shell, probes, tests, and GUI around the primary domain workflow.

## Changes

- Strategy Lab accepts normalized domains only.
- API and shell reject bare IPv4/IPv6, URL, host:port, single-label, and malformed inputs.
- Removed implicit IP TCP/443 behavior that lacked explicit DNS/SNI/port semantics.
- Retained domain endpoint expansion, including Telegram required endpoints.
- Added focused and integration regression coverage.

## Distribution

- `VERSION=0.3.2`;
- `PLUGIN_REVISION=23`;
- package candidate: `os-zapret2-restyle-0.3.2_23.pkg`;
- no release publication is authorized.
