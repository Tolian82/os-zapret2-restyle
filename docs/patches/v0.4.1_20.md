# v0.4.1_20 — Laboratory native perimeter/grid corrective

**Date:** 2026-08-15  
**Status:** SOURCE CANDIDATE — AUTOMATED/PUBLICATION ACCEPTANCE PENDING

## Owner-selected scope

Published `_19` fixed the requested RU/EN mode/status/navigation strings, but the owner-live comparison against the Strategy page and a native OPNsense settings page rejected the remaining Laboratory layout.

The visible defects are:

- the working-area perimeter is inset farther than ordinary OPNsense pages;
- the Diagnostics/Laboratory form value column does not follow the normal OPNsense form grid;
- `Режим:` must be guaranteed to use the same actual typography as the target label.

Durable predecessor evidence: [`../verification/evidence/2026-08-15-v0.4.1_19-laboratory-layout-owner-live-followup.md`](../verification/evidence/2026-08-15-v0.4.1_19-laboratory-layout-owner-live-followup.md).

## Implementation

- bump `PLUGIN_REVISION` to `20`;
- use one shared `diagnostics-form-table` contract for both the top domain-connectivity row and Strategy Lab inputs;
- use a native-style `25%` field-label column rather than the rejected fixed `250px` Laboratory label column;
- keep RU `Заблокированный домен / IP` on one line at normal UI typography;
- synchronize `Режим:` / `Mode:` font size and line height from the target field-label computed style;
- neutralize the nested Laboratory `page-content-main/container-fluid/row/col-xs-12` perimeter spacing so it does not add a second page inset;
- retain `_19` localization, Strategy Lab runtime/search semantics, Generic UDP, QUIC execution and Enable QUIC persistence behavior unchanged.

## Regression boundary

The focused Diagnostics localization/layout test now rejects:

- fixed `250px` Laboratory label columns;
- a returned `12px` typography workaround;
- loss of the shared 25% native form grid;
- loss of the perimeter-neutralization contract;
- loss of exact mode-label typography synchronization;
- regressions in RU/EN mode/status/navigation, circular idle text or persisted Enable QUIC source behavior.

## Package identity

- `VERSION=0.4.1`
- `PLUGIN_REVISION=20`
- target testing asset: `os-zapret2-restyle-0.4.1_20.pkg`
- required ABI: `FreeBSD:15:amd64`

## Required acceptance

Before source merge:

1. focused Diagnostics/Laboratory layout/localization/persistence test PASS;
2. complete project and Strategy Lab corrective matrix PASS;
3. FreeBSD-15 package build/inspection qualification PASS;
4. exact latest-head source merge.

After source merge:

1. persistent testing prerelease `v0.4.1_20` and package digest verification;
2. publication-record reconciliation;
3. one owner-live visual comparison confirming native OPNsense perimeter, common input column and matched `Режим:` typography.

After `_20` visual acceptance, the next engineering plan remains Laboratory target support for IP addresses as well as domains.
