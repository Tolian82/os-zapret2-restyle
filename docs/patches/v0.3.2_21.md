# Patch candidate `v0.3.2_21` — Semantic Strategy Lab restoration

## Scope

Corrective Patch 7 strengthens lifecycle snapshot and restoration verification. Circular eligibility remains Corrective Patch 8.

## Changes

- Added a production `strategy-lab-evidence` service action under the existing lifecycle lock.
- Captured normal service state, dvtws2 identity, supervisor identity, runtime args hash, effective configuration hash, and normal IPFW rule hash.
- Persisted the initial semantic snapshot in job state.
- Verified temporary candidate process and Strategy Lab IPFW cleanup.
- Restored the initial RUNNING or STOPPED state.
- Compared post-restore process, runtime, effective strategy, and firewall evidence with the initial snapshot.
- Persisted a structured restoration verdict.
- Converted every semantic mismatch into restoration failure.

## Regression coverage

The focused semantic-restoration test covers matching RUNNING restoration, initial STOPPED preservation, effective strategy mutation, temporary firewall residue, structured evidence, and production action wiring.

## Distribution

- `VERSION=0.3.2`;
- `PLUGIN_REVISION=21`;
- package candidate: `os-zapret2-restyle-0.3.2_21.pkg`;
- no release publication is authorized.
