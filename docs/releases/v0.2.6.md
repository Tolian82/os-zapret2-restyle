# v0.2.6

Release date: 2026-08-01

## Fixed

- Register `os-zapret2-restyle` in OPNsense firmware state from the package `+POST_INSTALL` hook so direct `pkg add` and package upgrades no longer leave the plugin marked `(misconfigured)` in the Plugins GUI.
- Invoke `/usr/local/etc/rc.configure_plugins` with the correct `POST_INSTALL` lifecycle mode instead of passing the plugin name as an unsupported mode.
- Extend the package lifecycle contract test to enforce both registration steps and prevent regression.

## Distribution

- Project version: `0.2.6`
- Package revision: `1`
- Expected package: `os-zapret2-restyle-0.2.6_1.pkg`
- Expected tag: `v0.2.6`
