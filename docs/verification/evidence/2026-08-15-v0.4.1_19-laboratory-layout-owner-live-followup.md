# v0.4.1_19 Laboratory layout owner-live follow-up

**Date:** 2026-08-15  
**Package:** `os-zapret2-restyle-0.4.1_19.pkg`  
**Status:** OWNER-LIVE PARTIAL — TEXT/PRESENTATION ITEMS VISIBLE; LAYOUT REJECTED; `_20` SELECTED

## Owner-live observation

The owner installed published `_19` and supplied three screenshots for direct comparison:

1. the current Zapret DPI Bypass Laboratory page;
2. the Zapret DPI Bypass Strategy page showing the normal plugin-page perimeter/grid;
3. a native OPNsense password/settings page showing the normal platform form perimeter/grid.

Visible `_19` presentation items include RU `Режим:`, `Расширенный` and `Статус: ожидание`. The owner did **not** accept the Laboratory layout as complete.

## Remaining defects selected for `_20`

- Laboratory input/value controls still do not sit on the normal OPNsense form value-column position;
- the Diagnostics test-domain row and the Laboratory target/UDP/QUIC rows should use the same native-style value-column alignment rather than a fixed `250px` Laboratory-only label column;
- `Режим:` must use the same actual font size/line height as `Заблокированный домен / IP`;
- the Laboratory page has a visibly larger inset around the whole working area than the Strategy page and ordinary OPNsense pages; the nested page/container/row/column wrapper must not add a second perimeter inset.

## `_20` corrective contract

`_20` is selected as a narrow GUI/layout corrective:

- use a shared `25%` label column for both Diagnostics input tables so their value controls follow the native OPNsense form grid;
- keep the long RU target label on one line without a reduced-font workaround;
- synchronize `Режим:` / `Mode:` computed font size and line height from the same field-label reference used by the target row;
- neutralize the nested Laboratory page/container/row/column padding/margins so only the ordinary OPNsense page perimeter remains;
- preserve all `_19` RU/EN strings, Strategy Lab runtime/search behavior, QUIC/UDP behavior and persisted Enable QUIC source contract.

No Strategy Lab execution rerun is required to accept this correction. The selected live proof is visual comparison of the Laboratory page against the Strategy/native OPNsense perimeter and field grid.
