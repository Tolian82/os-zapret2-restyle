# v0.3.3_5 live scenario 01 — candidate runtime / restoration failure

Date: 2026-08-07
Tester: repository owner
Platform: OPNsense 26.7.1_1 / FreeBSD 15 amd64
Package: `os-zapret2-restyle-0.3.3_5`
Target: `rutracker.org`
Mode: standard

## Initial state

The installed package identity was confirmed as `os-zapret2-restyle-0.3.3_5`, ABI `FreeBSD:15:amd64`, annotation `FreeBSD_version: 1500068`.

`configctl zapret status` reported the service running.

Semantic evidence before the test correctly reported the daemon-managed runtime:

```json
{"schema":1,"source":"zapret_service","state":"RUNNING","child_running":true,"supervisor_running":true,"runtime_args_hash":"c5fede51792a23c60407f43d428bdc0cb564c3c0bc7a2bda3f513871889ee78d","effective_config_hash":"a6898ccbbc85229fd1abd3581b3b8bf903a45ad68ef6da9a582eb6f54de926ec","normal_firewall_hash":"ea214cdec373b9a52a796123ee24daa1fc47f22e354acd72d1b23c0121bc2a4d"}
```

This confirms that the `_5` PID-file EOF correction fixed the previous false `INCOMPLETE` classification at stage 10.

## Automated Strategy Lab result

Stages 00, 10, 20, 30, and 40 passed.

Stage 50 failed with:

```text
Temporary candidate runtime failed internally.
```

Stages 60, 70, 80, and 85 were skipped.

Stage 90 failed with:

```text
RESTORE_FAILED — Исходное состояние Zapret2 восстановить не удалось.
```

Stage 99 also reported `RESTORE_FAILED`.

The Diagnostics UI final outcome was restoration failure.

## Final state

After the test:

```text
configctl zapret status
zapret is in an incomplete runtime state
```

Semantic evidence after the test:

```json
{"schema":1,"source":"zapret_service","state":"INCOMPLETE","child_running":false,"supervisor_running":false,"runtime_args_hash":"c5fede51792a23c60407f43d428bdc0cb564c3c0bc7a2bda3f513871889ee78d","effective_config_hash":"a6898ccbbc85229fd1abd3581b3b8bf903a45ad68ef6da9a582eb6f54de926ec","normal_firewall_hash":"ea214cdec373b9a52a796123ee24daa1fc47f22e354acd72d1b23c0121bc2a4d"}
```

The saved/effective hashes remained unchanged, but the original running child and supervisor were not restored.

## Conclusion

Scenario 1 remains **FAILED / PENDING CORRECTION** for `_5`.

The semantic PID/process inspection defect is fixed. The next confirmed defect is later in the lifecycle: a temporary candidate runtime fails internally at stage 50, and stage 90 cannot restore the original RUNNING Zapret2 runtime.

Do not continue dependent live Strategy Lab scenarios until this restoration path is corrected and a new FreeBSD 15 candidate passes scenario 1.
