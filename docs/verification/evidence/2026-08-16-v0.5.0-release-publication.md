# v0.5.0 full release publication

Status: **FULL RELEASE PASS — GitHub Release + FreeBSD 15 Pages/pkg repository published**

Date: 2026-08-16

## Immutable release identity

- project version: `0.5.0`;
- package revision: `_1`;
- package: `os-zapret2-restyle-0.5.0_1.pkg`;
- ABI: `FreeBSD:15:amd64`;
- release-preparation PR: `#267`;
- exact release-preparation merge: `d5afa6b1f4cfd7bc00e8e95d6896af8a1456fb24`;
- exact-head pre-merge CI / FreeBSD-15 qualification: `31915884270`, PASS;
- post-merge main-integrity CI: `31916249982`, main-integrity PASS;
- release trigger: `31916249900`, PASS;
- semantic tag: `v0.5.0`;
- annotated tag object: `069927d0d69986ce454cf41f5316d2509b94cc24`;
- tag target: exact release-preparation merge `d5afa6b1f4cfd7bc00e8e95d6896af8a1456fb24`;
- full release workflow: `31916256043`, PASS;
- GitHub Release ID: `371188698`;
- GitHub Release is stable (`draft=false`, `prerelease=false`).

## Release assets

GitHub Release `v0.5.0` contains:

- `os-zapret2-restyle-0.5.0_1.pkg`;
- `SHA256SUMS`.

Package SHA-256:

`38777bdf59f93e6cee596e431d01fef4b3a73a41842d93e809ba94fd310a5bce`

The uploaded package asset reports the same digest. `SHA256SUMS` contains the same package checksum.

## Pages/pkg repository

Release workflow job **Publish pkg repository** completed successfully and deployed GitHub Pages from release commit `d5afa6b1f4cfd7bc00e8e95d6896af8a1456fb24`.

Pages base URL:

`https://tolian82.github.io/os-zapret2-restyle/`

The exact deployed Pages artifact is workflow artifact `9254982156` (`github-pages`), artifact digest:

`sha256:b4e643803e5afb8cea4f6d7fb6da3ac6d514e40ae0c238f9970a78df38e56490`

The deployment artifact contains and the release workflow verified:

- `zapret2-restyle.conf`;
- `FreeBSD:15:amd64/os-zapret2-restyle-0.5.0_1.pkg`;
- `FreeBSD:15:amd64/SHA256SUMS`;
- `FreeBSD:15:amd64/meta.conf`;
- `FreeBSD:15:amd64/data.pkg`;
- `FreeBSD:15:amd64/packagesite.pkg`.

The package in the deployed Pages artifact has SHA-256:

`38777bdf59f93e6cee596e431d01fef4b3a73a41842d93e809ba94fd310a5bce`

`zapret2-restyle.conf` points the OPNsense/pkg client at:

`https://tolian82.github.io/os-zapret2-restyle/FreeBSD:15:amd64`

with the project-approved `signature_type: "none"` and priority `20`.

GitHub reports the Pages site as public, workflow-built and HTTPS-enforced. The release deployment job reported successful Pages deployment to the project Pages URL.

## Release conclusion

`v0.5.0` / `os-zapret2-restyle-0.5.0_1.pkg` is the current stable Web/pkg release. The full release boundary is complete: exact source identity, FreeBSD-15 package build, stable GitHub Release assets, checksum, and the matching Pages/pkg repository were all published from the same release-preparation merge.
