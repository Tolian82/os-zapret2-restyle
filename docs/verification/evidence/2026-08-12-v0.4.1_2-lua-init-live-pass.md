# v0.4.1_2 Lua initialization owner-live pass

Status: **PASS**

Owner appliance evidence was collected from installed `os-zapret2-restyle-0.4.1_2` on OPNsense / FreeBSD 15 amd64.

## Installed package / service

- package: `os-zapret2-restyle-0.4.1_2`;
- `configctl zapret status`: service RUNNING;
- measurement command: packaged `strategy_lab_python.py lua-init-measure`;
- production Model C was not changed or executed by the measurement.

## Corrected installed-resource evidence

The first invocation used the measurement-only command's then-default Lua directory `/usr/local/share/zapret2/lua` and therefore reported the three Zapret2 Lua resources as absent. This was a measurement-path defect only: the canonical Strategy Lab ResourceInventory uses `/usr/local/etc/zapret2/lua`.

The owner reran the same non-destructive measurement with:

`--lua-dir /usr/local/etc/zapret2/lua`

That corrected run reported:

- `policy=lua-init-set-equivalence-v1`;
- `candidate_count=16`;
- `all_candidates_same_dependencies=true`;
- `checks.all_batches_equivalent=true`;
- `checks.all_required_files_present=true`;
- `checks.production_model_unchanged=true`;
- `runtime_comparison_required=false`;
- `conclusion=equivalent_init_set`;
- `next_step=close_lua_initialization_optimization_and_measure_blob_startup_rss`.

Installed effective Lua resources were present:

- `/usr/local/etc/zapret2/lua/zapret-lib.lua` — `92721` bytes;
- `/usr/local/etc/zapret2/lua/zapret-antidpi.lua` — `51782` bytes;
- `/usr/local/etc/zapret2/lua/zapret-auto.lua` — `20974` bytes;
- `/usr/local/opnsense/scripts/OPNsense/Zapret/strategy_lab_model_c.lua` — `1224` bytes.

All six current Model-C batches reported identical current and candidate-minimal initialization sets:

1. `zapret-lib.lua`;
2. `zapret-antidpi.lua`;
3. `zapret-auto.lua`;
4. `strategy_lab_model_c.lua`.

## Decision

Lua candidate-minimal initialization is closed as a valid negative optimization result. Current production Model C already uses the minimum Lua union compatible with the accepted shared one-worker dispatcher architecture, so there is no distinct Lua-init variant that justifies a runtime timing/RSS A/B test.

Do not change production Model C for Lua initialization. Correct the measurement-only default path in the next packaged measurement patch and proceed to the independent BLOB loading / startup / RSS tradeoff measurement.
