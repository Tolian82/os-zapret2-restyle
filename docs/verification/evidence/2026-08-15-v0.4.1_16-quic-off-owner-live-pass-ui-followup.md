# v0.4.1_16 owner-live — QUIC OFF execution PASS and UI/i18n follow-up

**Date:** 2026-08-15
**Package:** `os-zapret2-restyle-0.4.1_16.pkg` / `v0.4.1_16`
**Status:** OWNER-LIVE QUIC OFF EXECUTION PASS · PERSISTENCE STILL PENDING · UI/I18N FOLLOW-UP SELECTED

## Owner-live observation

The owner supplied two Extended-mode Strategy Lab screenshots for `www.youtube.com` using Generic UDP port `53` and the exact `udp-140.bin` payload.

The first screenshot has **Enable QUIC ON**. Stage 80 reports real QUIC candidate execution with four attempted IDs (`quic-fake-1`, `quic-fake-2`, `quic-ipfrag-8`, `quic-ipfrag-16`) and configured Generic UDP execution with the three current UDP IDs.

The second screenshot has **Enable QUIC OFF**. Stage 80 explicitly reports that QUIC strategy search is disabled while configured Generic UDP still runs all three current UDP candidates (`udp-ipfrag-8`, `udp-ipfrag-16`, `udp-ipfrag-32`). The job finishes `SUCCESS`, produces stable TLS/HTTP candidates, and Stage 90 reports successful Zapret2 restoration.

This is sufficient owner-live evidence that the explicit OFF state suppresses QUIC candidate execution without suppressing the independent UDP path.

It is **not** sufficient to close persistence across page reload/revisit because the screenshots do not prove that the OFF value survived a reload or a later page visit. That row remains pending.

## Newly selected UI/presentation work

The owner requests a focused RU/EN presentation cleanup. Record these as implementation requirements, not merely review notes:

1. Temporary circular validation idle block:
   - do not expose the raw JSON braces `{` / `}` in the ordinary UI;
   - ordinary UI should render the state as localized text rather than raw `{"state":"idle"}`;
   - target ordinary labels: RU `Состояние: ОЖИДАНИЕ`, EN `State: IDLE`;
   - raw machine JSON may remain only in an explicitly advanced/raw view if still useful.
2. `Full output (advanced)` must have RU/EN presentation; RU target: `Полный вывод (расширенный)`.
3. `Enter a domain and click Test to check HTTPS connectivity.` must have RU/EN presentation; RU target: `Введите домен и нажмите «Проверка», чтобы проверить HTTPS-соединение.`
4. Table/result labels must be localized:
   - `Family` -> RU `Семейство`;
   - `Endpoints` -> RU `Назначения`;
   - `Outcome` -> RU `Результат`;
   - `Restoration` -> RU `Восстановление`;
   - `Replay` -> RU `Ответы`;
   - `Complete Traffic Strategy profile` -> RU `Полный профиль Traffic Strategy`.
5. Main action/button and section labels:
   - `Run` -> RU `Запуск`;
   - `Test Domain Connectivity` -> RU `Тестирование соединения с доменом`;
   - `Blocked Domain` becomes EN `Blocked Domain / IP`, RU `Заблокированный домен / IP`;
   - `Enable QUIC` -> RU `Включить QUIC`.
6. English mode must retain the corresponding English labels rather than leaking Russian strings.
7. Final acceptance must check both RU and EN modes in the live GUI, including the circular idle block and Strategy Lab result table.

Obvious spelling slips in the conversational request (`Rub`, `QUIQ`, `Отыеты`, `Включиь`) are normalized above to the intended product strings `Run`, `QUIC`, `Ответы`, `Включить`.

## Current boundary

Accepted:

- Enable QUIC ON real candidate execution;
- Enable QUIC OFF execution semantics;
- Generic UDP exact-byte path;
- Stage-90 visible restoration in these tested flows.

Still pending:

- Enable QUIC default/OFF persistence across reload/revisit;
- the UI/i18n implementation listed above;
- live RU/EN acceptance after implementation.
