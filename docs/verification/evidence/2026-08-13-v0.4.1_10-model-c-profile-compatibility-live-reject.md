# v0.4.1_10 Model-C compatibility evidence

Status: **MEASUREMENT REJECTED / PRODUCTION BATCHING DEFECT CONFIRMED**

Owner-live lifecycle measurement reached production Model C. Repeats 1-2 completed through Model C. Repeat 3 kept width three because no early winners had reduced the remaining winner target, and a later ready batch crossed the existing Model-C profile boundary. Model C correctly rejected it with `Model C bucket candidates are not profile-compatible` and replayed through Model B.

Root cause: the generic adaptive batch chooser preserves search order but does not enforce Model C's profile key `(l3, transport, port, l7, target_binding)`. The observed mixed batch crossed `target_binding`. This is a production Model-C batching defect, not a measurement, adapter, source-port lease, BLOB-loading, or candidate-classification defect.

The five-repeat lifecycle measurement remains rejected. The next corrective must keep native ordering and maximum width three while selecting only a contiguous profile-compatible ready prefix for Model C. Model B/A fallback stays unchanged.
