# v0.4.0_25 — owner-live source-port lease PASS

Status: **PASS**
Date: 2026-08-12
Package: `os-zapret2-restyle-0.4.0_25`
Runtime commit: `a5ecfbfd57820e30e5f2be450e510b96c00267e3`
Owner-live job: `job.5yGde5`
Target: `telegram.org`
Mode: Extended

## Purpose

Close the `_25` owner-live gate for the Stage-60 controlled source-port lease corrective.
The preceding `_23` Extended `telegram.org` run `job.G0wC5l` reproduced a shared static
`42004` collision through Model C and Model B, forcing cold Model A and ending Stage 60 with
`TIMEOUT` after 13 candidates. `_25` leases exact free concrete ports per admitted warm batch.

## Appliance result

The owner installed `os-zapret2-restyle-0.4.0_25` on OPNsense / FreeBSD 15 amd64.
Pre-test Zapret2 state was RUNNING and dedicated temporary rules `19128-19130` were absent.

Extended `telegram.org`, `job.5yGde5` completed with:

- Stage 60 execution model: `C-warm-bucket-source-port-dispatch`;
- `stopped_reason=graph_exhausted`;
- 16 candidates completed, zero winners;
- `.parallel.fallbacks=[]` — no Model-B or cold-Model-A fallback;
- Stage 60 duration: `34198 ms`;
- total job duration through Stage 99: `114759 ms`;
- final outcome: `NO_CANDIDATE`;
- Stage 90 restoration PASS;
- post-job `configctl zapret status`: RUNNING, pid `87046` at observation time;
- post-job dedicated temporary rules `19128-19130`: absent.

## Lease evidence

All six Stage-60 batches recorded the `_25` lease contract:

- `policy=preferred-free-else-alternate`;
- `foreign_port_action=skip-only`;
- exact `preferred_ports` and `leased_ports` were persisted for every candidate/endpoint;
- this run encountered no occupied preferred ports, therefore every batch recorded
  `collisions=[]`, `replacement_count=0`, `alternate_scan_count=0`.

The live run therefore proves that the `_25` lease wrapper is active in production Model C,
that the previous `_23` Telegram failure path no longer occurs, and that the full 16-candidate
no-winner graph completes without timeout or fallback on the same owner appliance.

This particular live run does **not** prove an alternate-port replacement under a real foreign
collision because no preferred port was occupied during the run. That replacement behavior
remains covered by the focused `_25` automated source-port lease contract; the owner-live gate
does not fabricate a collision that was not observed.

## Conclusion

`v0.4.0_25` source-port corrective owner-live gate: **PASS**.

The next Strategy Lab timing work is the separately documented adaptive-budget design based on
the actual eligible matrix:

`number of endpoints × IPv4/IPv6 × TLS/QUIC × Generic UDP × Standard/Extended mode`.

That follow-up must add finite proportional budget for real eligible work rather than replace
current limits with one guessed oversized static timeout.
