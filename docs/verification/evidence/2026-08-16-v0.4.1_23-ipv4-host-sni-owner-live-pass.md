# v0.4.1_23 IPv4 / Host-SNI owner-live closeout

Status: **OWNER-LIVE PASS — Laboratory IPv4 / Host-SNI target support complete**

Date: 2026-08-16

Package under test: `os-zapret2-restyle-0.4.1_23.pkg` / `v0.4.1_23`.

Owner-supplied closeout archive: `strategy-lab-v0.4.1_23-closeout-final.tgz`.

Archive SHA-256: `1b0cdaf071a3695f98ec7ee91b545de0fc255be2e3d69956e2d0b3e7e7d944a7`.

The archive contains 63 Strategy Lab job directories. The selected current-boundary jobs below are sufficient to close the `_23` truthful-result/domain/IPv4 verification matrix.

## Accepted jobs

### `job.9juf8H` — `rutracker.net`, Standard

- outcome: `SUCCESS`;
- final shortlist: 3 strategies;
- all three deep replays returned authenticated/intercepted HTTP `502` from the selected endpoint and were retained as `reachable_application_error` / `accepted=true`;
- selected and remote IP matched (`172.67.136.246`), endpoint attribution matched, and firewall interception was observed;
- Stage 90 restoration: `RUNNING -> RUNNING`, `verified=true`, `strategy_unchanged=true`, `temporary_runtime_clean=true`.

This directly closes the `_23` regression that previously erased Stage-70-stable finalists at Stage 85 solely because the application returned HTTP `4xx`/`5xx`.

### `job.TYjdSR` — `rutracker.org`, Standard

- outcome: `SUCCESS`;
- final shortlist: 3 strategies;
- exact deep replay remained accepted with HTTP `301` and endpoint/firewall attribution intact;
- Stage 90 restoration verified cleanly.

This closes the ordinary-domain positive regression row after the IPv4/Host-SNI work.

### `job.2Cx6Rc` — `telegram.org`, Standard

- outcome: `NO_CANDIDATE` without structural failure;
- Stage 90 restoration verified cleanly.

This is retained as an additional ordinary-domain negative regression showing that a legitimate no-candidate result remains truthful and lifecycle-safe.

### `job.3HeCEP` — bare IPv4 `37.48.102.131`, Standard

The owner first reproduced curl certificate identity failure with exit code `60` against the raw IP.

Strategy Lab then produced:

- outcome: `PARTIAL`;
- message: TLS certificate identity cannot be validated for the IPv4 target without a service name and Host/SNI should be supplied;
- final shortlist: 0, as expected for an incomplete HTTPS identity rather than a proven DPI-bypass failure;
- Stage 90 restoration verified cleanly.

This closes the bare-IP certificate-identity classification row and confirms that the old misleading `NO_CANDIDATE` result is gone.

### `job.HNnp5P` — bare IPv4 `91.105.192.1`, Extended, QUIC enabled, Generic UDP/53

- QUIC: `status=skipped`, `reason=host_sni_required`, `tested=[]`;
- therefore no fake four-candidate QUIC execution is reported for a raw IPv4 target without Host/SNI;
- Generic UDP remained independent: destination port `53`, payload size 4 bytes, direct control executed, and all three current UDP candidates (`udp-ipfrag-8`, `udp-ipfrag-16`, `udp-ipfrag-32`) were executed;
- Stage 90 restoration: `RUNNING -> RUNNING`, `verified=true`, `strategy_unchanged=true`, `temporary_runtime_clean=true`;
- the owner's post-run `ipfw` query for Strategy Lab range `19100..19131` returned no residual rules.

This closes the bare-IP QUIC skip, Generic UDP independence, and cleanup/restoration rows.

### `job.W0lKTv` — IPv4 `172.67.182.196` + Host/SNI `rutracker.org`, Extended, QUIC enabled

- outcome: `SUCCESS`;
- final shortlist: 1 stable strategy;
- service identity persisted as `rutracker.org` while destination remained fixed to `172.67.182.196`;
- exact profile replay used `--resolve rutracker.org:443:172.67.182.196` and recorded `selected_ip=remote_ip=172.67.182.196`, endpoint match true and firewall interception true;
- final profile contains `--ipset-ip=172.67.182.196`;
- exact replay was verified and the deep result was accepted;
- QUIC was not skipped: all four current candidates were actually executed (`quic-fake-1`, `quic-fake-2`, `quic-ipfrag-8`, `quic-ipfrag-16`);
- each QUIC probe connected to fixed IP `172.67.182.196` while using `-servername rutracker.org` and `-verify_hostname rutracker.org`; no QUIC candidate succeeded on the tested path, which is a valid negative protocol result rather than a contract failure;
- Stage 90 restoration: `RUNNING -> RUNNING`, `verified=true`, `strategy_unchanged=true`, `temporary_runtime_clean=true`.

This closes the fixed-destination + Host/SNI identity row, final `--ipset-ip=` profile/replay row, and Host/SNI QUIC execution row.

## Conclusion

The owner-live `_23` closeout matrix passes the selected Laboratory target-support boundary:

- ordinary domains did not regress;
- HTTP application `4xx`/`5xx` no longer destroys otherwise valid DPI-path evidence;
- bare IPv4 certificate identity failure is truthful `PARTIAL` guidance;
- bare IPv4 QUIC is skipped before execution without Host/SNI;
- Generic UDP remains independent for bare IPv4;
- IPv4 + Host/SNI keeps destination IP and service identity separate and produces an exact `--ipset-ip=` final profile/replay;
- Host/SNI QUIC performs real fixed-IP hostname-verified attempts;
- lifecycle restoration remains clean on all selected closeout jobs.

**Laboratory IPv4 / Host-SNI target support is complete and accepted.**

IPv6 target input remains intentionally deferred as a separate future scope. Enable QUIC OFF/default persistence across an actual page reload/revisit remains a separate pre-existing live acceptance row and is not part of this closeout.
