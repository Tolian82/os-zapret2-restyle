# v0.4.1_6 discovery cleanup-finalizer corrective — publication and owner-live PASS

Status: **ACCEPTED / OWNER-LIVE PASS**
Date: 2026-08-13

## Scope

This record closes the `_6` discovery measurement cleanup-finalizer corrective. The corrective changes only the measurement finalizer boolean boundary; production Model C, Model B, Model A, the native search graph, source-port dispatch and the production bounded GET-4K discovery policy remain unchanged.

## Persistent testing-package publication

The repository-owned testing-package publisher completed successfully for the exact `_6` source commit.

- source / tag target: `9a76551d17289fa6a125c10fabedf87241d1a490`;
- source tree: `36019521fb74c1736a9c6da822780133df639820`;
- testing tag: `v0.4.1_6`;
- publication workflow: `.github/workflows/publish-prerelease.yml`;
- publication workflow run: `31689302668` / #43 — SUCCESS;
- GitHub Release ID: `369818027`;
- release state: `draft=false`, `prerelease=true`;
- package asset ID: `512818044`;
- package asset: `os-zapret2-restyle-0.4.1_6.pkg`;
- package size: `188907` bytes;
- package digest: `sha256:e708d2ac0eb13d41d1d79da96e2b5f1f6e9d4fc9e138366fd4e72e30b96a02b7`;
- direct package URL: `https://github.com/Tolian82/os-zapret2-restyle/releases/download/v0.4.1_6/os-zapret2-restyle-0.4.1_6.pkg`;
- the workflow verified the FreeBSD 15 package manifest and the published prerelease contract;
- temporary branch `publish/v0.4.1_6` was deleted by the successful workflow;
- no GitHub Pages or pkg-repository promotion occurred.

## Corrected owner-live measurement

Reference job:

- `job.lWLjqL` / `rutracker.org`;
- retained production Model-C reference from the `_5` multidomain set;
- the reference run had previously completed Stage 60 in `24204 ms`, 16/16 candidates, winners `seqovl-host` and `seqovl-midsld`, `graph_exhausted`, with no fallback.

Owner command:

```text
/usr/local/opnsense/scripts/OPNsense/Zapret/strategy_lab_discovery_probe_measurement.sh job.lWLjqL /root/strategy-lab-v0.4.1_6-rutracker.json 10
```

Observed result:

```text
Discovery probe measurement report: /root/strategy-lab-v0.4.1_6-rutracker.json
Discovery probe measurement conclusion: measurement_accepted
```

This is the exact outcome the `_6` corrective was designed to restore. Under the measurement finalizer contract, `measurement_accepted` is not possible when cleanup remains false; therefore this accepted run confirms that the numeric shell cleanup-success value is now decoded successfully rather than being deterministically collapsed to false as in `_5`.

The owner did not paste the full JSON report in this turn, so this record does not invent or duplicate individual JSON fields beyond what is implied by the accepted final conclusion. The retained report path is recorded above.

## Post-run residue and lifecycle verification

Immediately after the accepted run the owner executed the required live cleanup checks.

Reserved Strategy Lab firewall range:

```text
/sbin/ipfw -a list | /usr/bin/awk '$1 >= 19100 && $1 <= 19131 {print}'
```

Result: no output.

Candidate `dvtws2` process on port 9989:

```text
/bin/ps ax -o pid= -o command= | /usr/bin/grep '[d]vtws2.*--port=9989'
```

Result: no output.

Candidate listening socket on port 9989:

```text
/usr/bin/sockstat -4 -l | /usr/bin/awk '$0 ~ /[:.]9989([[:space:]]|$)/ {print}'
```

Result: no output.

Normal Zapret2 service:

```text
configctl zapret status
zapret is running as pid 52091
```

Therefore the live appliance finished with no reserved Strategy Lab firewall residue, no candidate worker/listener residue, and the normal Zapret2 service restored and running.

## Decision

The `_6` corrective is **accepted** for its intended scope.

- cleanup-finalizer defect: CLOSED;
- corrected owner-live measurement: PASS / `measurement_accepted`;
- temporary firewall/process/socket cleanup: PASS;
- normal-service restoration: PASS;
- production Model C: unchanged;
- production discovery GET-4K: unchanged;
- `production_change_recommended=false` remains the decision from the multidomain `_5` experiment because HEAD and GET-1 did not show a material reproducible cost advantage over GET-4K.

No further repeat of this harness corrective is required unless a new regression appears. The discovery-probe optimization question is closed without a production policy change.