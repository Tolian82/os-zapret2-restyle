# v0.4.1_13 initial-Zapret2-STOPPED owner-live pass

Status: **PASS**

Owner appliance evidence was collected on the installed `os-zapret2-restyle-0.4.1_13` package for the risk-selected Strategy Lab row whose initial normal Zapret2 state is STOPPED.

## Scenario

- target: `rutracker.org`;
- mode: Standard;
- job: `job.5b97u9`;
- initial normal Zapret2 state: STOPPED;
- terminal GUI state: `ЗАВЕРШЕНО`;
- terminal result: `SUCCESS`;
- stable working strategies reported: `3`.

The purpose of this row was not to repeat the already accepted Model-C candidate/result telemetry. It was to prove that Strategy Lab can start and complete normally when the permanent Zapret2 service is not running, while preserving that original service state after the job.

## Post-job observation

The owner reported immediately after completion:

```text
root@OPNsense:~ # configctl zapret status
zapret is not running
root@OPNsense:~ # pgrep -af 'dvtws2|zapret.*supervisor'
```

The process query produced no output.

Thus the observed lifecycle path was:

`STOPPED -> Strategy Lab runs -> SUCCESS with 3 stable strategies -> STOPPED`

with no normal `dvtws2` or Zapret2 supervisor process left running after completion.

## Owner acceptance boundary

On 2026-08-15 the owner explicitly accepted this row as passed and rejected further repeated/deeper verification for the same scenario as unnecessary. For this selected regression row, the sufficient evidence is therefore the observable behavior above: Strategy Lab started from STOPPED, produced a normal successful result, and left the permanent service STOPPED after completion.

No telemetry archive, firewall hash, socket inventory, repeat run, or additional post-job inspection is required to accept this already completed row. This is a row-specific owner acceptance decision; it does not rewrite unrelated lifecycle contracts or fabricate unobserved evidence.

## Decision

Accept `job.5b97u9` as PASS for **Standard blocked domain, initial Zapret2 STOPPED**.

No product/source/package defect is indicated and no new package candidate is justified. The next selected risk-based coverage moves to a materially different path: one Extended blocked-domain run covering TLS 1.2 and HTTP behavior.