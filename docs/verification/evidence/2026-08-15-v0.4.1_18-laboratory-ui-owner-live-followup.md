# v0.4.1_18 — Laboratory UI owner-live follow-up

**Date:** 2026-08-15  
**Status:** OWNER-LIVE PARTIAL — `_19` CORRECTIVE SELECTED

## Live evidence supplied by owner

The owner installed the published `v0.4.1_18` testing package and supplied a Russian-mode Laboratory screenshot.

Visible acceptance on that screen:

- `Лаборатория стратегий` is translated correctly;
- `UDP порт (опционально)` is translated correctly;
- `Заблокированный домен / IP` remains on one line.

## Visible defects / owner corrections

The same live screenshot shows that the `_18` layout workaround is not acceptable as final presentation:

- the blocked-domain label is visibly smaller because `_18` forced `font-size:12px`;
- the owner reports the second and third left-side controls as shifted relative to the domain row and requires one explicit shared value-column x-position for domain / Generic UDP / Enable QUIC;
- mode selector RU values must be `Стандартный` and `Расширенный`; EN must remain `Standard` and `Extended`;
- immediately before the mode selector add a right-aligned RU `Режим:` / EN `Mode:` label;
- ordinary Strategy Lab idle must be RU `ожидание`; EN remains `idle`;
- Zapret sidebar child items must become EN `Strategy` / `Laboratory`, RU `Стратегия` / `Лаборатория`.

These are presentation/localization changes only. This owner-live evidence does not show a Strategy Lab search/runtime defect.

## Owner plan corrections accepted in the same handoff

The owner also declared these plan facts current:

- `already-accessible target` is complete;
- Settings Apply validation/guards are complete;
- Settings Apply post-Apply service-state correctness is complete;
- the next new product plan after the current Laboratory UI corrective is to let Laboratory test IP addresses as well as domains.

These statements are recorded as owner-confirmed project state; no additional hidden/live evidence is inferred by this record.

## Selected next patch

`v0.4.1_19` is selected to correct the typography/alignment and complete the requested RU/EN mode/status/sidebar presentation without changing Strategy Lab search/runtime semantics.
