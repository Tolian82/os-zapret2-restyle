# v0.4.1_13 Model-C-only owner-live regression pass

Status: **PASS**

Owner appliance evidence was collected from installed `os-zapret2-restyle-0.4.1_13` on OPNsense / FreeBSD 15 amd64. The owner supplied one telemetry archive containing three completed normal Strategy Lab jobs.

## Package identity

- package: `os-zapret2-restyle-0.4.1_13`;
- origin: `opnsense/os-zapret2-restyle`;
- architecture: `FreeBSD:15:amd64`;
- normal production Stage 60: Model C only.

## Accepted jobs

### `telegram.org` — `job.6RhNa1`

- terminal outcome: `NO_CANDIDATE`;
- direct TLS baseline inaccessible;
- Stage 60 completed `16/16` candidates;
- `stopped_reason=graph_exhausted`;
- shortlist count: `0`;
- Stage 60 duration: `37186 ms`;
- persisted evidence includes `model_c_only=true` and `.fallbacks=[]`;
- restoration: verified `RUNNING -> RUNNING`, strategy unchanged, temporary runtime clean.

This is the accepted exhaustive no-winner path.

### `rutracker.org` — `job.PEEjoY`

- terminal outcome: `SUCCESS`;
- direct TLS baseline inaccessible;
- Stage 60 completed `16/16` candidates;
- `stopped_reason=graph_exhausted`;
- stable shortlist count: `3`;
- shortlist: `03-seqovl`, `seqovl-host`, `seqovl-midsld`;
- Stage 60 duration: `26191 ms`;
- persisted evidence includes `model_c_only=true` and `.fallbacks=[]`;
- restoration: verified `RUNNING -> RUNNING`, strategy unchanged, temporary runtime clean.

This is the accepted blocked-target full-search success path.

### `www.youtube.com` — `job.7Kz5ro`

- terminal outcome: `SUCCESS`;
- direct TLS baseline inaccessible in the measured environment;
- Stage 60 stopped after `7/16` candidates;
- `stopped_reason=enough_candidates`;
- stable shortlist count: `3`;
- shortlist: `multidisorder-midsld`, `seqovl-midsld`, `golden-owner-multisplit-fake-tls-7`;
- Stage 60 duration: `7173 ms`;
- persisted evidence includes `model_c_only=true` and `.fallbacks=[]`;
- restoration: verified `RUNNING -> RUNNING`, strategy unchanged, temporary runtime clean.

This is the accepted early-success path.

## Restoration and containment

All three jobs persisted:

- `.restoration.verified=true`;
- `initial_state=RUNNING`;
- `final_state=RUNNING`;
- `strategy_unchanged=true`;
- `temporary_runtime_clean=true`.

No automatic Model B/A replay appears in the retained Stage-60 evidence. The three jobs jointly cover exhaustive no-candidate, exhaustive success, and early-success behavior under the published `_13` Model-C-only production route.

## Decision

Accept the published `_13` owner-live gate as PASS.

The selected `v0.4.1_13` Model-C-only production finalization is complete. No source/package correction is justified by these runs, so package identity remains unchanged.

The next accepted risk-selected Strategy Lab regression row is **Standard blocked domain with initial Zapret2 STOPPED**. Its required invariant is exact restoration to STOPPED after the job; it is a new regression-coverage task, not a reopening of the already-closed Lua/BLOB/discovery/model-selection experiments.
