# v0.4.1_11 owner-live lifecycle evidence — warm readiness root cause

Status: **ROOT CAUSE CONFIRMED / INPUT TO v0.4.1_12**

The owner installed `os-zapret2-restyle-0.4.1_11.pkg` and ran five retained Model-C lifecycle repeats against `job.lWLjqL`.

Observed measurement integrity:

- requested repeats: 5;
- completed repeats: 5;
- `fallback_detected=false` for every repeat;
- `model_c_only=true` for every repeat;
- `instrumented_batch_count_match=true` for every repeat;
- `lifecycle_metrics_complete=true` for every repeat;
- RSS complete for every repeat;
- cleanup and lifecycle restoration passed.

The report was formally rejected only because live candidate PASS/FAIL outcomes varied across repeats, which in turn changed the adaptive logical-batch sequence. That variability is retained as network/result diagnostics and is not evidence of a Model-C infrastructure fallback.

The performance evidence exposed a separate deterministic source defect. A normal one-segment Model-C batch repeatedly reported `pool_startup_ms` around `1056–1066 ms`; logical batches containing two sequential profile segments reported approximately `2116–2126 ms`. Source inspection showed the shared warm-worker readiness helper required two qualifying snapshots but slept exactly one second between attempts.

The accepted earlier BLOB startup/RSS measurement used the same process/socket/log readiness predicates, required two consecutive qualifying snapshots, polled every 25 ms for at most four seconds, and measured stable readiness around 63 ms. Therefore the approximately one-second production startup component is polling granularity, not measured dvtws2 initialization cost.

Corrective target: retain the two-snapshot/four-second safety proof while replacing one-second readiness polling with the accepted 25 ms cadence. No search, candidate, routing or fallback semantic change is justified by this evidence.
