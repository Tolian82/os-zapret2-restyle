# v0.4.0_7 — owner-live Stage-60 completion and late-stage timing evidence

Date: 2026-08-10
Candidate: `v0.4.0_7` / `os-zapret2-restyle-0.4.0_7.pkg`
Target: `telegram.org`
Owner environment: OPNsense / FreeBSD 15 amd64

## Runs

The owner supplied both complete Strategy Lab job directories plus a post-run system
snapshot:

- Standard: `job.RFVs75`;
- Extended: `job.QbUuYO`.

Both runs finished `state=completed`, `outcome=NO_CANDIDATE` with Stage 90 PASS.
The installed package was `0.4.0_7`, the normal Zapret2 service was running after the
runs, and the post-run IPFW snapshot contained only the normal rule `19000`; no
Strategy Lab rule in `19100-19131` remained.

## Stage-60 acceptance

The direct `_7` acceptance target passed in both modes:

| Mode | Stage 50 | Stage 60 | Stage-60 duration | Candidates |
|---|---|---|---:|---:|
| Standard | PASS | PASS | 90.243 s | 16/16 |
| Extended | PASS | PASS | 89.249 s | 16/16 |

The previous local 70-second Stage-60 timeout is therefore closed. The 16-node graph
completed without a parent termination and the later stages executed normally.

## Late-stage telemetry selected for `_8`

The same jobs provide the first clean timing evidence after Stage 60 became reachable:

### Standard `job.RFVs75`

- Stage 70: 1.058 s with only 13 seconds of parent budget exposed by the orchestrator;
- Stage 80: intentionally SKIPPED in Standard mode;
- Stage 85: 0.202 s, but `operation_timeout_seconds=null`;
- Stage 90 restoration: 6.921 s, also with `operation_timeout_seconds=null`;
- total job time through mandatory restoration: 145.181 s.

### Extended `job.QbUuYO`

- Stage 70: 1.056 s with 14 seconds of Standard-search budget remaining;
- Stage 80 TCP: 23.177 s with a 120-second shared-stage envelope;
- Stage 80 QUIC: 1.156 s with 97 seconds remaining; capability result was `skipped`;
- Stage 80 configured UDP: 1.155 s with 96 seconds remaining; input result was `skipped`;
- Stage 85: 0.203 s, but `operation_timeout_seconds=null`;
- Stage 90 restoration: 6.945 s, also with `operation_timeout_seconds=null`;
- total job time through mandatory restoration: 169.427 s.

No Stage-70 candidate was available in either run, so the jobs prove the empty-source
path but not safe admission of a real three-attempt stability candidate near the Standard
deadline. The Extended run likewise proves the normal Stage-80 action envelope but not a
candidate launched close to the shared Stage-80 deadline.

## Source diagnosis

The source boundary after `_7` is asymmetric:

- Stage 60 performs candidate admission before every cold candidate and can persist
  `partial=true` / `stopped_reason=insufficient_stage_budget` before returning timeout;
- Stage 70 still enters stability attempts without the same admission test;
- Stage-80 TCP/QUIC/UDP loops still enter a candidate without checking that the full
  candidate execution/termination/cleanup envelope fits the remaining shared Stage-80
  action budget;
- Stage 85 first checks that some overall budget remains, but its adapter invocation has
  no explicit parent timeout;
- Stage 90 mandatory restoration is intentionally outside exhausted search budget, but
  the Python parent also has no explicit bound even though the shell lifecycle itself is
  internally bounded.

This evidence selects `_8`: close the remaining `_32` late-stage containment boundary
without changing `_33` discovery, fail-fast stability, or finalist deep-validation
semantics.
