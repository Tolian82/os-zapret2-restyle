# v0.4.0_26 — testing prerelease publication

Status: **PUBLISHED; OWNER-LIVE VERIFICATION PENDING**
Date: `2026-08-12`

## Source identity

- tag: `v0.4.0_26`;
- target commit: `8ada9cba28916fff506f19b34f5ef3de16e2008e`;
- target tree: `170c54cb8b8a354e4052898ea5db8b1e36a1bb61`;
- package identity: `os-zapret2-restyle-0.4.0_26.pkg`;
- ABI: `FreeBSD:15:amd64`.

The target commit is the verified squash merge of PR `#182`, `v0.4.0_26: Add adaptive Strategy Lab budget`.

## Source CI

Latest mergeable PR head:
`dce900d291b86d35b3ca173fe949973c1301fe4d`.

The PR head and final main commit have the same tree:
`170c54cb8b8a354e4052898ea5db8b1e36a1bb61`.

Required PR checks on the latest head passed:

- Pull request title — SUCCESS;
- CI run `31583257998` — SUCCESS;
- Validate Project — SUCCESS;
- Build FreeBSD 15 package — SUCCESS.

The PR CI artifact was `os-zapret2-restyle-0.4.0_26`, artifact id `9136236447`, with workflow ZIP digest
`sha256:5b7abd19bf00e60a6a000425979b1d6d36b09e9c8f6e42969b4d45c290e029f2`.
This ZIP digest is not the published `.pkg` digest.

## Publication

Publication used the repository-owned `Publish testing prerelease` workflow on temporary branch
`publish/v0.4.0_26`, pinned to the exact main commit above.

- workflow run: `31584348303`;
- conclusion: `SUCCESS`;
- release id: `369135019`;
- `draft=false`;
- `prerelease=true`;
- published at `2026-08-12T09:46:18Z`;
- temporary publication branch deleted automatically after verified success;
- GitHub Pages and the pkg repository were not published by this workflow.

Published asset:

- name: `os-zapret2-restyle-0.4.0_26.pkg`;
- asset id: `511384034`;
- state: `uploaded`;
- size: `180306` bytes;
- digest: `sha256:f5466c21c014bf594afcc80aac49b948db45513b33fe46d4857eded75bc8af8c`;
- direct URL: `https://github.com/Tolian82/os-zapret2-restyle/releases/download/v0.4.0_26/os-zapret2-restyle-0.4.0_26.pkg`.

## Live boundary

Publication alone does not close `_26`.

The selected owner-live gate remains one Extended `telegram.org` run proving production
`policy=eligible-work-v1` wiring after Stage 30:

- `adaptive-budget.json` exists;
- its work matrix matches the actual endpoint/IPv6/QUIC/Generic UDP state;
- effective seconds equal the deterministic `_26` formula;
- telemetry records `phase=budget_adaptation`, Stage `30`, `outcome=pass`;
- Stage 60 follows the normal Model-C path without an unexpected fallback;
- no new timeout occurs;
- Stage 90 restoration succeeds with no temporary rule/process residue.

The latest accepted owner-live runtime remains `_25` until that gate passes.
