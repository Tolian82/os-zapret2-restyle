# v0.4.1_23 Enable QUIC preference persistence owner-live pass

Status: **OWNER-LIVE PASS — persistence row closed**

Date: 2026-08-16

Package under test: `os-zapret2-restyle-0.4.1_23.pkg` / `v0.4.1_23`.

## Owner confirmation

After the persistence boundary was explained explicitly as the saved **Enable QUIC** checkbox surviving a real Laboratory page reload/revisit, the owner confirmed that the **Enable QUIC state works as intended**.

This closes the previously pending owner-live row covering the model-backed default/load/save path for the Strategy Lab QUIC preference.

The already accepted `_16` execution evidence remains the separate proof that QUIC OFF suppresses the QUIC candidate catalog while independent Generic UDP can continue. The `_23` IPv4/Host-SNI closeout separately proves truthful bare-IP QUIC skip and real Host/SNI QUIC attempts.

## Conclusion

- default remains OFF (`enablequic` model default `0`);
- the owner confirms the saved checkbox state persists correctly through the real Laboratory reload/revisit workflow;
- no source correction is required;
- this backlog row is complete.
