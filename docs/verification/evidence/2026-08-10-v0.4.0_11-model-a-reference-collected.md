# v0.4.0_11 — Model A cold reference collected

Date: 2026-08-10
Tester: repository owner
Package: `os-zapret2-restyle-0.4.0_11.pkg`
Job: `job.TtZeaH`
Target: `rutracker.org`
Mode: Standard
Outcome: `SUCCESS`
Model A conclusion: `reference_collected`

## Appliance identity

The owner-generated Model A report records:

- OPNsense `26.7.1_1 (amd64)`;
- FreeBSD `15.1-RELEASE-p1` / `amd64`;
- installed package version `0.4.0_11`;
- 108 installed external fake/BLOB files in the recorded ResourceInventory.

The job completed from initial `RUNNING` service state back to final `RUNNING` with
`restoration.verified=true`, `strategy_unchanged=true`, and
`temporary_runtime_clean=true`.

## Model A coverage

The read-only `model-a summarize` report consumed the retained Stage 50/60/70/85 cold
candidate evidence and produced 25 samples. Every machine-checkable cold-reference gate
passed:

- known PASS observed: `true`;
- known FAIL observed: `true`;
- repeated candidate observed: `true`;
- required resource classes observed: `blob-free`, `builtin`, `external`;
- output ranges observed: `-d8`, `-d10`;
- overlapping TLS/443 candidate coverage: 16 unique specs;
- candidate RSS observed: `true`;
- restoration verified for all summarized jobs: `true`;
- missing coverage checks: none.

Therefore the report returned:

`conclusion=reference_collected`.

## Cold timing reference

Across 25 cold candidate samples:

| Phase | Median | p90 | Max |
|---|---:|---:|---:|
| prepare | 140 ms | 160 ms | 165 ms |
| launch | 17 ms | 20 ms | 21 ms |
| readiness | 1046 ms | 1052 ms | 1138 ms |
| probe | 220 ms | 2045 ms | 2055 ms |
| stop + cleanup | 81 ms | 102 ms | 109 ms |
| total candidate | 1580 ms | 3411 ms | 3463 ms |

The result is consistent with the first `_10` cold timing run: readiness remains a stable
roughly one-second fixed cost, while the large total-time tail is dominated by failing
network probes near their bounded probe deadline.

## RSS reference

All 25 candidate samples now contain numeric `rss_kb`, proving the `_11` propagation
correction on the owner appliance.

Observed resident-set values:

- minimum: 4316 KiB;
- median: 4332 KiB;
- p90: 4348 KiB;
- maximum: 4356 KiB.

This is approximately 4.2 MiB resident memory for one cold candidate `dvtws2` after
readiness on this appliance/runtime. RSS is a measurement of the candidate process, not
of the complete Strategy Lab job or the OPNsense system.

## Candidate/equivalence evidence

The sample set contains both working and failing TLS 1.3 candidates, repeated 3/3
stability executions, final cold replays, an external-resource `-d8` candidate, and exact
endpoint/IPFW interception evidence. Working samples recorded endpoint match and
interception for the pinned `104.21.32.39` endpoint; failed samples remained classified
as failures rather than being promoted by interception alone.

The retained repeated stable candidates include `multidisorder-host`, `03-seqovl`, and
`seqovl-host`, each with four samples across discovery/stability or replay evidence.

## Accepted boundary

Model A is now accepted as the cold correctness/performance reference for the A/B/C
runtime experiment program.

This result does **not** approve a warm runtime model. It only unlocks the next experiment:
Model B coexistence of multiple warm `dvtws2` workers with distinct validated process and
divert ownership, while candidate probes remain sequential.

Model B must still prove exact candidate attribution, Model-A result equivalence,
independent worker stop behavior, aggregate/per-worker RSS, cancellation cleanup and zero
Stage-90 residue before any production decision. True parallel candidate probes and Model
C/source-port dispatch remain out of scope.

## Measurement limitations retained

- `resource_init_ms` remains unavailable while it cannot be separated from
  launch/readiness;
- teardown is still recorded as combined `stop_cleanup_ms` rather than independent stop
  and cleanup values;
- this evidence selects no new timeout and changes no production search/lifecycle policy.

## Previous evidence

The preceding `_10` appliance run exposed only the missing RSS propagation and is retained
at:

`docs/verification/evidence/2026-08-10-v0.4.0_10-model-a-rss-gap.md`.
