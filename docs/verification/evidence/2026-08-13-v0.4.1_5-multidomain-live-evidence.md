# v0.4.1_5 — multidomain Model-C / discovery-probe owner-live evidence

Status: **PARTIAL LIVE EVIDENCE / DETAILS PENDING**
Date: 2026-08-13
Package: `os-zapret2-restyle-0.4.1_5`
Testing tag: `v0.4.1_5`

## Purpose

Preserve the owner-live data collected while broadening the Strategy Lab performance review beyond the retained `telegram.org` worst-case reference.

The owner explicitly wants the Model-C and discovery-probe conclusions compared across three different target shapes:

1. `telegram.org` — retained worst-case / long-search target expected to exhaust the graph and produce no working candidate;
2. `rutracker.org` — realistic intermediate target where a working candidate may require a stronger strategy and may require BLOB-backed mechanisms;
3. `www.youtube.com` — expected by the owner to be a simpler working target, potentially succeeding without an external BLOB.

The second and third statements above are test hypotheses, not accepted results. Production remains unchanged until the persisted live evidence is fully extracted and compared.

## Installed package / initial live state

The owner installed the persistent GitHub package:

`os-zapret2-restyle-0.4.1_5.pkg`

Normal Zapret2 service was observed RUNNING before and after the discovery measurement series.

## Telegram worst-case discovery measurement

Reference job: `job.xhdgCU`
Target: `telegram.org`
Reference role: retained worst-case `NO_CANDIDATE / graph_exhausted` production basis
Output file on owner appliance:

`/root/strategy-lab-v0.4.1_5-discovery-probe.json`

Observed report identity:

- `schema=1`;
- `policy=discovery-probe-agreement-v1`;
- `candidate_count=10`;
- `sample_count=40`;
- `conclusion=measurement_rejected`.

Checks:

- `expected_sample_count=true`;
- `single_search_epoch=true`;
- `endpoint_attribution_complete=true`;
- `lifecycle_restored=true`;
- `cleanup_ok=false`.

Agreement versus `deep-16k`:

| variant | comparable | agreement | ratio | false PASS | false FAIL | inconclusive |
|---|---:|---:|---:|---:|---:|---:|
| `head` | 10 | 10 | 1.0 | 0 | 0 | 0 |
| `get-1` | 10 | 10 | 1.0 | 0 | 0 | 0 |
| `get-4k` | 10 | 10 | 1.0 | 0 | 0 | 0 |

Every deep reference sample was FAIL in this Telegram corpus:

- `deep-16k`: 10 FAIL, 0 PASS, 0 inconclusive;
- `head`: 10 FAIL, 0 PASS, 0 inconclusive;
- `get-1`: 10 FAIL, 0 PASS, 0 inconclusive;
- `get-4k`: 10 FAIL, 0 PASS, 0 inconclusive.

Timing summary from the owner-supplied report:

| variant | probe mean ms | probe median ms | probe p90 ms | total mean ms | total median ms | total p90 ms |
|---|---:|---:|---:|---:|---:|---:|
| `deep-16k` | 4111.2 | 4106.0 | 4140 | 5497.9 | 5488.5 | 5505 |
| `head` | 4101.5 | 4099.0 | 4142 | 5486.0 | 5459.5 | 5503 |
| `get-1` | 4104.0 | 4097.5 | 4140 | 5464.8 | 5467.5 | 5494 |
| `get-4k` | 4109.6 | 4110.5 | 4142 | 5474.3 | 5473.5 | 5521 |

Observed ranges / variability retained from the report:

- `deep-16k` probe min/max `4081/4147 ms`, stdev `23.47 ms`; total min/max `5447/5672 ms`, stdev `64.587 ms`;
- `head` probe min/max `4057/4167 ms`, stdev `35.513 ms`; total min/max `5403/5736 ms`, stdev `92.411 ms`;
- `get-1` probe min/max `4077/4163 ms`, stdev `29.698 ms`; total min/max `5423/5517 ms`, stdev `28.244 ms`;
- `get-4k` probe min/max `4058/4167 ms`, stdev `38.991 ms`; total min/max `5408/5584 ms`, stdev `54.201 ms`.

Because all ten deep-reference candidates failed, this Telegram run is primarily worst-case FAIL-path evidence. It must not by itself decide the useful latency of a cheaper probe on successful candidates.

After the measurement, `configctl zapret status` reported RUNNING, pid `75659` at observation time.

## Telegram cleanup follow-up

The initial report recorded `cleanup_ok=false`. A direct post-run cleanup inspection then produced:

- `/bin/sh .../strategy_lab_candidate_adapter.sh cleanup job.xhdgCU` returned shell status `0`;
- no IPFW rules in reserved range `19100-19131` were present;
- no `dvtws2` process using `--port=9989` was present;
- no IPv4 listening divert socket on `9989` was present;
- normal Zapret2 remained RUNNING, pid `75659` at observation time.

Therefore the report's `cleanup_ok=false` is not, by itself, evidence of persistent candidate-runtime or firewall residue. The reason the measurement wrapper recorded `false` remains unresolved and requires more granular cleanup evidence before the harness is accepted.

A previous diagnostic command that appeared to show a `dvtws2` PID was a false match against the `awk` command line itself and is not retained as process evidence.

## Fresh rutracker.org job

Owner identified the new reference job as:

`job.lWLjqL`

Target context: `rutracker.org`.

The intended comparison run was requested in Standard mode, but the exact persisted mode and Stage-60 fields have not yet been extracted from the job JSON in this evidence record.

The attempted multi-line `jq` command was not csh-safe and failed before reading the JSON (`Unmatched '''`, command-not-found / badly-placed-parentheses errors). Those shell errors are **not** Model-C results and must never be treated as runtime evidence.

Post-job observations that did complete successfully:

- `configctl zapret status` — RUNNING, pid `48083` at observation time;
- no dedicated temporary IPFW rules `19128-19130` remained.

Discovery-probe measurement command produced:

`/root/strategy-lab-v0.4.1_5-rutracker.json`

and reported:

`Discovery probe measurement conclusion: measurement_rejected`

The detailed `.checks`, `.comparisons`, `.summaries`, per-sample classifications and timings have not yet been copied from the owner appliance into project evidence.

## Fresh www.youtube.com job

Owner identified the new reference job as:

`job.NAYQxm`

Target context: `www.youtube.com`.

As with the rutracker run, the intended comparison run was requested in Standard mode, but the exact persisted mode and Stage-60 fields have not yet been extracted into this evidence record.

The attempted multi-line `jq` command failed at the csh parsing layer before JSON evaluation. Those shell errors are not Model-C evidence.

Post-job observations that did complete successfully:

- `configctl zapret status` — RUNNING, pid `97243` at observation time;
- no dedicated temporary IPFW rules `19128-19130` remained.

Discovery-probe measurement command produced:

`/root/strategy-lab-v0.4.1_5-youtube.json`

and reported:

`Discovery probe measurement conclusion: measurement_rejected`

The detailed `.checks`, `.comparisons`, `.summaries`, per-sample classifications and timings have not yet been copied from the owner appliance into project evidence.

## Current interpretation boundary

This evidence intentionally records only what the owner console output proves.

What is already established:

- Telegram provides useful worst-case FAIL-path timing for all four probe variants;
- Telegram cheap/deep classification agreement was 10/10 for each cheap variant in that all-FAIL corpus;
- the Telegram wrapper report rejected on `cleanup_ok=false` even though subsequent direct cleanup/residue inspection was clean;
- fresh rutracker and YouTube Strategy Lab jobs now exist and their normal service restoration / dedicated-rule cleanup were observed;
- both fresh `_5` discovery measurements completed collection but reported `measurement_rejected`.

What is **not** yet established from the supplied output:

- the exact Model-C `execution_model`, `stopped_reason`, `completed`, winner list or fallback list for `job.lWLjqL` and `job.NAYQxm`;
- whether either fresh domain supplied `deep-16k` PASS samples in the first ten selected candidates;
- HEAD / GET-1 / GET-4K latency advantage on PASS candidates;
- the exact reason either fresh discovery report was rejected;
- whether the first-ten corpus is sufficient for rutracker or needs expansion to all sixteen native TLS 1.3 candidates.

No production change is authorized by this partial record. Production Stage 60 remains:

`C-warm-bucket-source-port-dispatch -> B-warm-worker-parallel-batched -> A-cold-fallback`

and production discovery remains the bounded 4 KiB GET.

## Required follow-up evidence

Preserve and extract, before retention removes the fresh jobs:

- `job.lWLjqL/status.json`;
- `job.lWLjqL/parameter-expansion.json`;
- `job.NAYQxm/status.json`;
- `job.NAYQxm/parameter-expansion.json`;
- `/root/strategy-lab-v0.4.1_5-rutracker.json`;
- `/root/strategy-lab-v0.4.1_5-youtube.json`;
- the already collected `/root/strategy-lab-v0.4.1_5-discovery-probe.json`.

The final multidomain comparison should separate deep-reference PASS and FAIL candidates and compare agreement, false PASS/FAIL, probe/total timing, Model-C execution/fallback behavior, and lifecycle/cleanup evidence for all three targets.
