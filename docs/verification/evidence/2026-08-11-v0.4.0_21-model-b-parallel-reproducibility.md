# v0.4.0_21 — Model B controlled-parallel owner-live reproducibility

Date: 2026-08-11
Package: `os-zapret2-restyle-0.4.0_21.pkg`
Reference job: `job.tMYnFA`
Target: `telegram.org`
Mode: Standard
Reference outcome: `NO_CANDIDATE`
Reference Stage-60 stop reason: `graph_exhausted`
Reference corpus: 16 candidates / 2 pinned endpoints / 32 endpoint probes
Model: `B-warm-worker-parallel-batched`
Experiment only: yes
Production approval at time of measurement: no

## Appliance context

Owner appliance reported:

- OPNsense package ABI: `FreeBSD:15:amd64`;
- logical CPUs: 2;
- CPU model: `Westmere E56xx/L56xx/X56xx (Nehalem-C)`;
- CPU count is measurement metadata only and does not define the architecture width.

The package was installed successfully and `configctl zapret status` reported the normal
Zapret service running after the repeat series.

## Accepted `_21` live run

The first owner-live `_21` run returned:

- `conclusion=accept`;
- `preliminary_accept=true`;
- every required check true, including `route_attribution`, `result_equivalence`,
  `candidate_parallelism_observed`, `concurrency_limit_respected`,
  `endpoints_sequential_per_candidate`, `cleanup_between_batches`, and
  `restoration_verified`;
- `parallel_exhaustive_search_ms=33108`;
- `candidate_runtime_speedup_percent=62.805` versus the retained 89012 ms cold
  Stage-60 candidate runtime;
- `projected_full_job_speedup_percent=38.552` with
  `projection_is_measured_full_job=false`;
- peak aggregate warm-worker RSS 12988 KiB.

All five full three-worker batches observed `max_overlap_observed=3`; the final one-worker
batch observed width 1. All batch cleanup checks passed.

## Five-run reproducibility series

The owner then executed five unchanged `_21` parallel exhaustive runs against the same
retained reference job.

| Run | Harness real s | Parallel exhaustive ms | Candidate-runtime speedup % | Projected full-job speedup % | Peak RSS KiB | Result |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| 1 | 42.49 | 33107 | 62.806 | 38.553 | 12988 | ACCEPT |
| 2 | 42.43 | 33018 | 62.906 | 38.614 | 13000 | ACCEPT |
| 3 | 42.33 | 32933 | 63.002 | 38.673 | 12992 | ACCEPT |
| 4 | 42.33 | 32953 | 62.979 | 38.659 | 12996 | ACCEPT |
| 5 | 42.48 | 33117 | 62.795 | 38.546 | 12988 | ACCEPT |

Every repeat reported:

- `conclusion=accept`;
- `preliminary_accept=true`;
- `route_attribution=true`;
- `candidate_parallelism_observed=true`;
- `restoration.verified=true`.

Repeat-series statistics:

- parallel exhaustive mean: **33025.6 ms**;
- median: **33018 ms**;
- minimum / maximum: **32933 / 33117 ms**;
- total min/max spread: **184 ms**, about **0.56%** of the mean;
- mean measured candidate-runtime speedup: **62.8976%** versus the retained
  89012 ms cold Stage-60 candidate runtime;
- mean projected full-job speedup: **38.609%**;
- mean peak aggregate RSS: **12992.8 KiB**;
- harness real-time mean: **42.412 s**.

Including the initial accepted `_21` run, the owner has six accepted controlled-parallel
runs with the same corpus and clean restoration.

## Comparison with accepted sequential Model B

The accepted `_19` sequential exhaustive five-run mean is 74808.2 ms. The `_21` repeat
series mean of 33025.6 ms is therefore about **55.85% lower** than the sequential warm
Model B exhaustive runtime.

The same `_21` mean is about **62.90% lower** than the retained 89012 ms cold Stage-60
candidate runtime.

The projected full Standard-job value is not a measured full Strategy Lab Model B job and
must not be presented as one.

## Cleanup / restoration evidence

After the five-run series:

- installed package: `os-zapret2-restyle-0.4.0_21`;
- `configctl zapret status` reported Zapret running as PID 68518;
- `ipfw show | grep -E '^(19128|19129|19130) '` returned no output;
- no dedicated Model B rule residue remained.

Each report independently recorded:

- `cleanup_ok=true`;
- initial service state `RUNNING`;
- final service state `RUNNING`;
- `normal_firewall_unchanged=true`;
- `strategy_unchanged=true`;
- `temporary_runtime_clean=true`;
- `restoration.verified=true`.

## Conclusion

**ACCEPT — reproducible controlled-parallel Model B evidence.**

The complete no-candidate corpus remained semantically equivalent to the cold reference,
three-way candidate overlap was repeatedly observed under the configured width, route
attribution remained deterministic after the `_21` failed-probe corrective, and cleanup /
semantic restoration remained clean.

This evidence is strong enough to select controlled-parallel Model B as the target
architecture for a separate production-integration patch. It does not by itself make the
existing experiment harness production code; production Model A remains authoritative
until that integration is implemented and passes its own CI/live gate.
