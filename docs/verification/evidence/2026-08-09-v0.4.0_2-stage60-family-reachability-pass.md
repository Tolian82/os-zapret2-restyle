# v0.4.0_2 — Adaptive-search `_28` live PASS

Date: 2026-08-09
Tester: repository owner
Target: `discord.com`
Mode: Standard
Diagnostic job: `job.2HVQqr`
Candidate: `v0.4.0_2` / `os-zapret2-restyle-0.4.0_2.pkg`
ABI: `FreeBSD:15:amd64`

## Package evidence

The owner installed the published testing prerelease. `pkg info` reported:

- name `os-zapret2-restyle`;
- version `0.4.0_2`;
- architecture `FreeBSD:15:amd64`;
- install time `Sun Aug 9 08:27:34 2026 MSK`.

## Focused `_28` evidence

The completed Standard job exercised the exact all-rejected Stage-50 path that `_28`
changed:

| Evidence | Owner-observed result |
|---|---|
| Stage 50 | PASS; `total=7`, `completed=7`, `accepted=[]`, all seven families rejected |
| Stage 60 | PASS; `total_available=14`, `completed=14`, `working=[]`, all 14 catalog candidates attempted and failed |
| Stage-60 stop reason | `catalog_exhausted`, not the obsolete `no_accepted_family` hard gate |
| Stage 70 | PASS; zero candidates available for stability replay |
| Stage 90 | PASS; temporary processes/rules removed and initially RUNNING Zapret2 restored healthy |
| Stage 99 | truthful `NO_CANDIDATE`, not an internal search error |

The Stage-60 event text also identified the active policy correctly:
`Expanding TLS 1.3 candidates using Stage-50 evidence for priority`.

The 14 attempted Stage-60 candidates covered every current catalog row:

- `multisplit-host`, `multisplit-midsld`;
- `multidisorder-host`, `multidisorder-midsld`;
- `seqovl-host`, `seqovl-midsld`;
- `fake-repeat2`, `fake-rnd`;
- `fake-split-host`, `fake-split-midsld`;
- `syndata-1603`, `syndata-160301`;
- `hostfakesplit-mid`, `hostfakesplit-disorder`.

## Restoration and residue evidence

After terminal completion:

- `configctl zapret status` reported `zapret is running as pid 69039`;
- the IPFW excerpt contained only the normal rule `19000`;
- no temporary rule from the reserved `19100–19131` range remained;
- `status.json` and the GUI both reported Stage 90 PASS and 100% terminal completion;
- no `RESTORE_FAILED` outcome was present.

The supplied `pgrep -af` excerpt contained numeric PIDs without command text, so this
record does not invent process identities from that output. Restoration is established by
the explicit Stage-90 result, healthy service status and absence of temporary firewall
rules.

## Qualification result

This run proves the `_28` contract on OPNsense: an empty Stage-50 `accepted` list changes
neither Stage-60 reachability nor bounded restoration. Stage 60 attempted the complete
catalog and terminated normally.

`NO_CANDIDATE` means the current fixed catalog did not find a stable strategy for
`discord.com`; it is not a failure of `_28`. Candidate representation, resource
inventory, adaptive graph search, timeout redesign and deep validation remain assigned to
`_29`–`_33`. The unrelated pending rows of the full live matrix are not claimed as PASS.

Result: **ADAPTIVE-SEARCH `_28` FOCUSED LIVE PASS on `v0.4.0_2`**.
