# v0.4.1_16 owner-live — Generic UDP configured-path PASS

**Date:** 2026-08-15
**Package:** `os-zapret2-restyle-0.4.1_16.pkg` / `v0.4.1_16`
**Status:** OWNER-LIVE PASS

## Correction of the earlier diagnosis

The earlier `_15`/early `_16` owner reports did not prove a browser, upload-directory, or filesystem-permissions defect. The owner later identified the input mistake: files described as "140" had been approximately **140 KiB**, while the documented Generic UDP payload limit is **1..4096 bytes**.

A controlled Windows PowerShell fixture was then created with an exact size of 140 bytes:

`[System.IO.File]::WriteAllBytes("$env:USERPROFILE\Desktop\udp-140.bin", [byte[]](0..139))`

`(Get-Item "$env:USERPROFILE\Desktop\udp-140.bin").Length` returned exactly `140`.

The owner selected that exact file in the published `_16` Diagnostics / Strategy Lab page with:

- target: `rutracker.org`;
- mode: Extended;
- Generic UDP port: `53`;
- payload: `udp-140.bin`, exactly `140` bytes;
- Enable QUIC: OFF.

The UI immediately showed the positive staged-file evidence:

`Файл подготовлен к отправке: udp-140.bin, 140 байт.`

## Live job result

Owner-live job: `job.j09XUc`.

Observed terminal behavior:

- overall result: `SUCCESS`;
- Stage 80: PASS;
- QUIC presentation: strategy search disabled, matching Enable QUIC OFF;
- Generic UDP configured path: port `53`, payload `140` bytes;
- selected endpoint/IP shown: `172.67.182.196`;
- direct control reply: not observed;
- UI explicitly states that no reply **does not mean the UDP port is closed**;
- three UDP candidates were actually executed:
  - `udp-ipfrag-8`;
  - `udp-ipfrag-16`;
  - `udp-ipfrag-32`;
- no working UDP strategy was found, which is a valid negative search result;
- Stage 90: PASS; temporary processes/rules removed and the original Zapret2 service restored/running;
- final shortlist still contained one stable TLS 1.3 candidate from the ordinary Strategy Lab path.

## Acceptance conclusion

The published `_16` Generic UDP browser-to-job path is accepted owner-live for the tested scenario.

Specifically proven live:

1. an exact valid 140-byte file reaches application-owned ready state;
2. the configured UDP job starts with the selected port and payload size;
3. Stage 80 exposes endpoint/IP and direct control observation;
4. UDP no-reply is not misclassified as a closed port;
5. the UDP candidate catalog executes despite direct no-reply;
6. actual candidate count/IDs are visible in normal UI;
7. a negative `no working strategy` result is reported truthfully;
8. Enable QUIC OFF is presented naturally in the same Stage-80 result;
9. Stage-90 service restoration is visibly PASS.

The earlier permissions/browser-upload hypothesis is therefore **not a confirmed product defect** for this case and must not remain as the current explanation. The strict payload limit remains **bytes**, not KiB: `1..4096 bytes`.

No new package is required for this documentation reconciliation; package identity remains `v0.4.1_16`.
