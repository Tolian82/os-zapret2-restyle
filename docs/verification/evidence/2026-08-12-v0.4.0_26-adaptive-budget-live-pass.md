# v0.4.0_26 — adaptive-budget owner-live PASS

Status: **PASS**
Date: `2026-08-12`
Package: `os-zapret2-restyle-0.4.0_26`
Target: `telegram.org`
Mode: `Extended`
Job: `job.xhdgCU`

## Purpose

This owner-live run closes the selected `_26` change gate by proving that the production
Stage-30 capability measurement is wired to `policy=eligible-work-v1`, that the persisted
work matrix and effective finite budgets match the actual appliance capabilities, and that
the accepted Model-C/restoration boundaries remain intact.

The run does not fabricate IPv6, QUIC or Generic UDP availability. Optional increments remain
deterministically covered by the focused `_26` source contract; this evidence verifies the
production wiring and the actually measured topology.

## Measured work matrix

`adaptive-budget.json` persisted:

- `schema=1`;
- `policy=eligible-work-v1`;
- `reference_endpoints=2`;
- mode `extended`;
- endpoint count `2`;
- IPv4 `true`;
- IPv6 `false`;
- TLS 1.3 `true`;
- Extended TCP `true`;
- QUIC/IPv4 `false`;
- Generic UDP `false`.

`status.json` independently reported network state:

- IPv4 `available`;
- IPv6 `unavailable`;
- QUIC/IPv4 `closed`;
- QUIC/IPv6 `skipped`;
- UDP request `configured=false`.

The persisted work matrix therefore matches the actual appliance capability/input state.

## Adaptive budget result

No optional work was eligible in this measured topology, so every adaptive addition was zero:

- extra-endpoint Standard `0 s`;
- extra-endpoint Extended `0 s`;
- IPv6 `0 s`;
- QUIC `0 s`;
- Generic UDP `0 s`.

Base and effective budgets were exactly:

- Standard `150 s`;
- Extended increment `120 s`;
- search/job parent `270 s`;
- Stage 80 `120 s`.

`status.json` persisted the same public numeric values:

- `standard_budget_seconds=150`;
- `extended_budget_seconds=120`;
- `search_budget_seconds=270`;
- `stage80_budget_seconds=120`.

The job start/deadline evidence was:

- `started_at=2026-08-12T10:31:09Z`;
- `standard_deadline_at=2026-08-12T10:33:39Z`;
- `deadline_at=2026-08-12T10:35:39Z`.

This confirms the accepted `_25` two-endpoint IPv4-only floor remains exactly
`150 + 120 = 270 s` rather than being replaced by one oversized static timeout.

## Budget telemetry

`timing-telemetry.json` contains a production adaptation record:

- `phase=budget_adaptation`;
- `stage=30`;
- `outcome=pass`;
- `duration_ms=0`;
- details repeat the same `eligible-work-v1` matrix, weights, zero additions and effective
  `150/120/270/120` seconds.

## Stage 60 / Model C

`parameter-expansion.json` recorded:

- `execution_model=C-warm-bucket-source-port-dispatch`;
- `stopped_reason=graph_exhausted`;
- `completed=16`;
- working candidates `[]`;
- `.parallel.fallbacks=[]`.

All Stage-60 batches remained `outcome=warm`. The accepted `_25` lease wrapper stayed active
with `policy=preferred-free-else-alternate` and `foreign_port_action=skip-only`; no preferred
port happened to be occupied in this run, so collisions/replacements remained zero.

The first three width-three batches explicitly persisted `physical_worker_count=1`. Later
batches did not repeat that optional summary field, but the top-level execution model remained
Model C throughout and no Model-B/cold-Model-A fallback occurred.

## Timing and final result

Relevant timing telemetry:

- Stage 60: `34209 ms`, PASS;
- Stage 80 records: `23191 ms`, `1157 ms`, `1156 ms`, all PASS;
- Stage 90: `6924 ms`, PASS;
- whole job: `114644 ms`, outcome `no_candidate`.

Final state:

- `state=completed`;
- `outcome=NO_CANDIDATE`;
- Stage 60 completed all 16 candidates without timeout;
- Stage 80 truthfully skipped unavailable QUIC and unconfigured Generic UDP;
- Stage 90 restoration PASS.

Post-job appliance checks:

- `configctl zapret status` — RUNNING, pid `78016` at observation time;
- no dedicated temporary rules `19128-19130` remained.

## Acceptance

The selected `_26` owner-live gate is **PASS**.

Production evidence now proves:

1. Stage-30 measurement activates `eligible-work-v1`;
2. the persisted matrix matches actual endpoint/capability/protocol eligibility;
3. the deterministic finite budget calculation is applied exactly;
4. no unnecessary budget is charged for unavailable IPv6/QUIC or unconfigured Generic UDP;
5. Stage 60 remains production Model C with no unexpected fallback;
6. no new stage/whole-job timeout is introduced on the accepted reference topology;
7. restoration and temporary-rule cleanup remain correct.

Optional IPv6/QUIC/Generic-UDP increments are not claimed as owner-live exercised by this run;
their deterministic arithmetic remains covered by `scripts/test-strategy-lab-adaptive-budget.sh`.
