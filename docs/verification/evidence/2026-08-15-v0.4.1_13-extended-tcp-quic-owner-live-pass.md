# v0.4.1_13 Extended TLS 1.2 / HTTP and QUIC-gating owner-live pass

Status: **PASS**

Owner appliance evidence was collected from installed `os-zapret2-restyle-0.4.1_13` on OPNsense / FreeBSD 15 amd64.

## Job

- target: `rutracker.org`;
- mode: Extended;
- job: `job.TJlWoY`;
- terminal state: `ЗАВЕРШЕНО`;
- terminal outcome: `SUCCESS`;
- stable shortlist count shown by GUI: `1`;
- Stage 80: `PASS`;
- Stage 90: `PASS`, normal Zapret2 service restored and operational.

## Extended TLS 1.2

The persisted `.extended.protocols.tls12` result proves that the branch executed rather than being skipped.

Two candidates were tested:

1. `tls12-multisplit`
   - family `multisplit`;
   - protocol `tls12`, TCP/443, L7 `tls`;
   - runtime became ready/stable with process identity, socket readiness and clean log verified;
   - firewall interception was observed;
   - probe ended with curl exit `28`, connection timeout;
   - `all_pass=false`.
2. `tls12-fake`
   - family `fake`;
   - protocol `tls12`, TCP/443, L7 `tls`;
   - uses builtin `fake_default_tls`;
   - runtime became ready/stable with process identity, socket readiness and clean log verified;
   - firewall interception was observed;
   - probe ended with curl exit `35`, connection reset by peer;
   - `all_pass=false`.

Persisted TLS-1.2 summary: `working=null`.

This is an accepted truthful negative protocol result: the Extended TLS-1.2 path ran correctly, attributed traffic through the temporary runtime, collected endpoint evidence and reported that neither catalog candidate worked for this target/environment.

## Extended HTTP

The persisted `.extended.protocols.http` result proves that the branch executed rather than being skipped.

Two candidates were tested:

1. `http-multisplit`
   - family `multisplit`;
   - protocol `http`, TCP/80, L7 `http`;
   - runtime became ready/stable with process identity, socket readiness and clean log verified;
   - selected endpoint IP matched the observed remote IP;
   - firewall interception was observed;
   - probe ended with curl exit `28`, zero-byte HTTP timeout;
   - `all_pass=false`.
2. `http-multidisorder`
   - family `multidisorder`;
   - protocol `http`, TCP/80, L7 `http`;
   - runtime became ready/stable with process identity, socket readiness and clean log verified;
   - selected endpoint IP matched the observed remote IP;
   - firewall interception was observed;
   - probe ended with curl exit `28`, zero-byte HTTP timeout;
   - `all_pass=false`.

Persisted HTTP summary: `working=null`.

This is an accepted truthful negative protocol result: the Extended HTTP path ran correctly, traffic was endpoint-bound/intercepted, and the result correctly records that neither catalog candidate worked for this target/environment.

## QUIC capability gating

Stage 30 reported IPv4 available, IPv6 unavailable and `QUIC/IPv4` closed. Stage 80 then completed with `QUIC=skipped`.

That is the intended capability-gating path: QUIC is not treated as a failed strategy when the network precheck says the capability is unavailable/closed; the Extended QUIC branch is explicitly skipped.

## Generic UDP

Stage 80 reported `UDP=skipped`. The GUI had a port value but no request payload file was supplied; the Generic UDP request therefore was not fully configured. This evidence does **not** close the configured Generic UDP row.

## Decision

Accept the following `_13` live-regression rows as PASS:

- Extended TLS 1.2 execution/result semantics;
- Extended HTTP execution/result semantics;
- QUIC capability gating when QUIC/IPv4 is closed.

No source/package defect is demonstrated. `working=null` for TLS 1.2 and HTTP is a target/environment result, not an orchestration failure, because the candidate runtimes reached ready/stable state, traffic interception/endpoint evidence was collected, Stage 80 completed PASS and the job restored normally.

The next selected materially different Strategy Lab row is **configured Generic UDP**. It requires both a valid UDP port and a 1-4096-byte request payload file so the UDP branch executes instead of reporting `skipped`.
