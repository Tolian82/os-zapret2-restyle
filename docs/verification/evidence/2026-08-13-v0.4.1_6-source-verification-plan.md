# v0.4.1_6 — cleanup finalizer source verification plan

Status: **PRE-MERGE VERIFICATION CONTRACT**
Date: 2026-08-13

The `_6` source corrective is intentionally limited to the `_5` measurement cleanup-boolean finalizer defect.

Required pre-merge checks:

- `VERSION` remains `0.4.1` and `PLUGIN_REVISION` is exactly `6`;
- the discovery measurement Python CLI accepts shell numeric cleanup success `1` and failure `0` without silently collapsing both to false;
- canonical textual `true`/`false` remains accepted;
- invalid boolean values fail closed;
- finalization with all other gates true plus cleanup success produces `measurement_accepted`;
- finalization with cleanup failure produces `measurement_rejected`;
- production discovery-policy immutability flags remain false;
- no Model-C, Model-B, Model-A, search-graph, source-port, candidate or request-policy source is changed by the patch;
- normal project validation and the FreeBSD 15 package build pass on the latest PR head.

Owner-live after installation must repeat at least one compatible discovery measurement and verify `checks.cleanup_ok=true`, lifecycle restoration, attribution and no candidate/firewall residue. This is the appliance confirmation for the finalizer correction, not authorization to change production GET-4K.
