# v0.4.0_19 Model B exhaustive reproducibility

Result: **OWNER-LIVE ACCEPT 5/5** for the experiment-only sequential exhaustive Model B benchmark.

Reference job: `job.tMYnFA` (`telegram.org`, Standard, `NO_CANDIDATE`, 16/16 Stage-60 `graph_exhausted`, two pinned endpoints).

All five `_19` exhaustive runs returned `conclusion=accept`, `error=null`, every required check true, and verified semantic restoration. Production Strategy Lab remained Model A.

Measured warm exhaustive search wall times:

- run 1: `74886 ms`, speedup versus cold Stage-60 candidate runtime `15.870%`, peak batch RSS `12992 KiB`;
- run 2: `74692 ms`, speedup `16.088%`, peak batch RSS `12988 KiB`;
- run 3: `75083 ms`, speedup `15.648%`, peak batch RSS `12988 KiB`;
- run 4: `74780 ms`, speedup `15.989%`, peak batch RSS `12980 KiB`;
- run 5: `74600 ms`, speedup `16.191%`, peak batch RSS `12976 KiB`.

Summary:

- mean warm exhaustive search: `74808.2 ms`;
- median: `74780 ms`;
- min/max: `74600 / 75083 ms`;
- total range: `483 ms`, about `0.65%` of the mean;
- mean measured Stage-60 candidate-runtime speedup: `15.957%`;
- cold Stage-60 candidate runtime in the retained reference: `89012 ms`;
- cold full-job total: `145010 ms`;
- `_19` full-job comparison remains a projection, not a measured Model B full Strategy Lab run.

This closes sequential exhaustive Model B reproducibility on the owner appliance. It does not approve production Model B. The next experiment selected by the approved experiment plan is controlled simultaneous candidate probing after deterministic coexistence has been proven.
