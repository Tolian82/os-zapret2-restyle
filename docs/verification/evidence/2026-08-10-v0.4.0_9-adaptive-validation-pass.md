# v0.4.0_9 adaptive-validation owner live evidence

Result: **ADAPTIVE-SEARCH `_33` CHANGE-SPECIFIC LIVE PASS on `v0.4.0_9`**, with one explicit coverage limitation: the live winner run exercised the strict 3/3 success path, but did not produce an unstable finalist that would trigger the fail-fast rejection branch. That rejection branch remains source-regression verified rather than owner-live observed.

## Appliance and package

Owner-supplied evidence bundle collected on 2026-08-10:

- OPNsense: `26.7.1_1` amd64;
- kernel: `FreeBSD 15.1-RELEASE-p1 stable/26.7`;
- package: `os-zapret2-restyle-0.4.0_9`;
- package architecture: `FreeBSD:15:amd64`;
- post-test `configctl zapret status`: running;
- post-test IPFW excerpt contained only the normal rule `19000`; no reserved Strategy Lab rule `19100-19131` remained.

The bundle contains three complete jobs:

- Standard no-winner: `job.tU3wiL`, target `telegram.org`;
- Extended no-winner: `job.hsP8Ro`, target `telegram.org`;
- Standard winner-path: `job.UPRDlc`, target `rutracker.org`.

## No-winner regression — telegram.org

Both Standard and Extended runs completed without timeout and terminated truthfully as `NO_CANDIDATE`.

### Standard `job.tU3wiL`

- Stage 50: PASS, about `38.993 s`;
- Stage 60: PASS, all `16` graph candidates checked, `stopped_reason=graph_exhausted`, about `89.247 s`;
- Stage 70: PASS with zero candidates, about `1.057 s`;
- Stage 80: skipped by Standard mode;
- Stage 85: PASS with empty shortlist, about `0.207 s`;
- Stage 90: PASS, restoration about `6.874 s`;
- total through mandatory restoration: about `144.125 s`;
- restoration evidence: initial `RUNNING` -> final `RUNNING`, `strategy_unchanged=true`, `temporary_runtime_clean=true`.

### Extended `job.hsP8Ro`

- Stage 50: PASS, about `38.979 s`;
- Stage 60: PASS, all `16` graph candidates checked, `stopped_reason=graph_exhausted`, about `89.307 s`;
- Stage 70: PASS with zero candidates, about `1.057 s`;
- Stage 80: PASS; TCP branch about `23.142 s`, QUIC branch about `1.157 s`, UDP branch about `1.156 s`; the observed capability/input state records QUIC and generic UDP as skipped rather than failed;
- Stage 85: PASS with empty shortlist, about `0.202 s`;
- Stage 90: PASS, restoration about `6.892 s`;
- total through mandatory restoration: about `169.262 s`;
- restoration evidence: initial `RUNNING` -> final `RUNNING`, `strategy_unchanged=true`, `temporary_runtime_clean=true`.

These two jobs show that `_33` preserves the `_32` timeout-containment/no-winner behavior in both modes.

## Winner-path regression — rutracker.org

Standard job `job.UPRDlc` reached the new `_33` stability and finalist-depth logic and completed as `SUCCESS`.

### Adaptive discovery

- Stage 50 completed in about `19.063 s` and accepted reconnaissance family `seqovl`;
- Stage 60 completed in about `16.272 s`;
- Stage 60 stopped intentionally with `stopped_reason=enough_candidates` after checking `6` candidates rather than exhausting the graph;
- three working candidates reached the stability stage;
- total job time through mandatory restoration was about `71.023 s`.

### Strict 3/3 stability

Stage 70 completed in about `16.051 s` and produced three stable candidates.

Each of the three candidates executed exactly three fresh attempts and passed all three. Telemetry records `validation_policy=fail_fast_3_of_3` on every attempt. Because no attempt failed, the fail-fast rejection branch was not triggered in this live run; no claim is made that this branch has owner-live coverage.

### Cold finalist validation

Stage 85 completed in about `5.269 s` and evaluated exactly three publication finalists. Each finalist received one cold exact-profile replay with:

- `attempt_count=1`;
- `pass_count=1`;
- `verified=true`;
- policy `single_deep_after_fail_fast_3_of_3`.

All three deep replays received a successful HTTP `301` response from the pinned endpoint but only `162` response bytes. The configured depth target was `16384` bytes, so all three were correctly classified:

- `classification=inconclusive`;
- `accepted=true`;
- `reason=resource_completed_short`.

This is the intended `_33` behavior: a short successful resource does not become a false 16-KiB PASS and does not erase independently proven 3/3 connectivity/stability evidence.

The three final profiles were:

1. `multidisorder` / TLS 1.3 — `--lua-desync=multidisorder:pos=host`;
2. `seqovl` / TLS 1.3 — `--lua-desync=multisplit:pos=1:seqovl=1`;
3. `seqovl` / TLS 1.3 — `--lua-desync=multisplit:pos=host:seqovl=1`.

All three profiles also contain the expected TCP/443, TLS, `rutracker.org`, `--out-range=-d10`, and TLS ClientHello selector lines.

### Restoration

Stage 90 completed in about `6.924 s`.

Persisted restoration evidence records:

- `verified=true`;
- initial state `RUNNING`;
- final state `RUNNING`;
- `strategy_unchanged=true`;
- `temporary_runtime_clean=true`.

The post-bundle appliance snapshot independently showed Zapret2 running and only normal IPFW rule `19000` in the inspected Strategy Lab range.

## Conclusion

The owner-live evidence closes the change-specific `_33` boundary for:

- bounded discovery preserving no-winner behavior in Standard and Extended modes;
- adaptive early stop after enough working candidates;
- strict fresh-connection 3/3 success validation;
- one cold exact-profile finalist replay per publication finalist;
- truthful `inconclusive` classification for a successful resource shorter than 16 KiB;
- mandatory cleanup/restoration after both no-winner and winner paths.

The fail-fast rejection branch remains automated-regression evidence only because the live winner set was stable 3/3. Warm-runtime Models B/C, dispatcher/source-port hypotheses and true parallel candidate probing remain experimental and are not approved by this result.

A separate UX backlog item tracks localization/presentation of idle state without changing the internal `state: idle` contract: issue #155.
