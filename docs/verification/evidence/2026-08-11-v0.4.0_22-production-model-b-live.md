# v0.4.0_22 production Stage-60 Model B owner-live evidence

Result: **PRODUCTION STAGE-60 MODEL B OWNER-LIVE PASS FOR THE STANDARD NO-CANDIDATE AND WORKING-CANDIDATE PATHS; EXTENDED FAIL-CLOSED COLD FALLBACK OBSERVED.**

## Evidence authority

This record is the current owner-live evidence for `v0.4.0_22` and supersedes historical
interpretations of Stage-60 behavior from earlier packages. In particular, the old fixed
Stage-60 timeout/partial-completion defect was already closed by `v0.4.0_7` and confirmed
through `v0.4.0_8`. A current `16/16` Stage-60 run is therefore not evidence that the old
defect returned.

Historical evidence remains useful for comparison, but current package/patch/PR/live records
must be read before drawing conclusions from an older run.

## Appliance and package

Owner-supplied live evidence collected on 2026-08-11:

- installed package: `os-zapret2-restyle-0.4.0_22`;
- package ABI: `FreeBSD:15:amd64`;
- production Stage-60 engine: `B-warm-worker-parallel-batched`;
- fixed maximum candidate width: `3`;
- post-test `configctl zapret status`: running (`pid 2604` in the supplied snapshot);
- post-test `ipfw show | grep -E '^(19128|19129|19130) '` returned no dedicated Model B rule residue.

## Standard no-candidate path — telegram.org / job.KpLHgb

The real Strategy Lab production path completed truthfully as `NO_CANDIDATE`.

Stage 60 persisted:

- `execution_model=B-warm-worker-parallel-batched`;
- `stopped_reason=graph_exhausted`;
- `completed=16`;
- `working=[]`;
- `early_stop.triggered=false`;
- `winner_count=0`;
- `warm_enabled=true`;
- `fallbacks=[]`.

Warm batches were widths `3,3,3,3,3,1`. Every width-three batch recorded
`max_overlap_observed=3`. Stage-60 adapter time was `34227 ms`; total job time through
finalization/restoration was `89039 ms`.

GUI evidence records Stage 60 PASS, zero Stage-70 candidates, an empty shortlist, terminal
`NO_CANDIDATE`, and Stage-90 restoration PASS.

This closes the owner-live production no-candidate / complete graph-exhaustion boundary for
`_22`.

## Standard working-candidate path — rutracker.org / job.GK0X66

The production Model B path remained warm for the complete Stage 60:

- `execution_model=B-warm-worker-parallel-batched`;
- `stopped_reason=graph_exhausted`;
- `completed=16`;
- `working=[seqovl-host,seqovl-midsld]`;
- `early_stop.triggered=false`;
- `winner_count=2`;
- `within_normal_band=true`;
- `warm_enabled=true`;
- `fallbacks=[]`.

The first batches exercised real candidate overlap at widths `3`, `2`, and `2`; later work
became width one as determined by the resolved adaptive frontier and remaining winner count.
Stage-60 adapter time was `28151 ms`; total job time was `81272 ms`.

The exact Stage-60 working candidates were:

1. `seqovl-host`;
2. `seqovl-midsld`.

The remaining fourteen Stage-60 candidates remained FAIL. Stage 70 nevertheless completed
with three stable candidates from the existing downstream input contract; Stage 85 produced
three final candidates; terminal outcome was `SUCCESS`; Stage 90 restoration was PASS.

### Correct interpretation of 16/16

The current Stage-60 winner target is three and the accepted normal winner band includes two.
This run found two Stage-60 winners, so `graph_exhausted` after `16/16` is truthful current
behavior. It must not be diagnosed from the historical `_9` run that happened to obtain
three winners and therefore stopped after six candidates.

The explicit `early_stop.triggered=true` branch was not exercised by this `_22` owner-live
set because no supplied `_22` run reached three Stage-60 winners. This is an evidence
coverage statement, not a defect claim.

## Extended fail-closed fallback — rutracker.org / job.d5XV82

The Extended run selected production Model B, but the first warm batch detected:

`controlled source port is already in use: 42003`

The designed fail-closed boundary then activated:

- `fallback_model=A-cold`;
- `warm_enabled=false` for the remainder of Stage 60;
- all 16 candidates still completed;
- the same Stage-60 working candidates remained `seqovl-host` and `seqovl-midsld`;
- Stage-60 adapter time was `35166 ms`;
- total job time was `100444 ms`;
- terminal result was `SUCCESS`;
- Stage 80 completed PASS;
- Stage 90 restoration completed PASS;
- no dedicated rules `19128-19130` remained after the run.

This is positive owner-live evidence for the fail-closed cold fallback and cleanup boundary.
It does **not** prove the `42003` collision itself is resolved. Treat the collision as an
observed condition to investigate only if it recurs or can be reproduced.

## Comparison with accepted parallel experiment evidence

The `_21` experiment series already established reproducible controlled-parallel mechanics:
5/5 unchanged ACCEPT repeats, mean parallel exhaustive search `33025.6 ms`, about `62.90%`
measured candidate-runtime reduction versus cold Model A and roughly 13 MiB peak aggregate
three-worker RSS.

The `_22` Standard no-candidate production run is consistent with that mechanism-level
result: real Stage 60 completed in `34227 ms` while using width-three warm batches with
observed overlap and no fallback.

## Cleanup and restoration

All supplied completed `_22` jobs reached successful Stage-90 restoration. The post-test
appliance snapshot independently shows the normal Zapret2 service running and no dedicated
Model B IPFW rules `19128-19130`.

## Evidence boundary

Owner-live verified on `_22`:

- real production Model B Stage-60 routing;
- complete Standard no-candidate graph exhaustion;
- Standard working-candidate production path;
- real width-three candidate overlap;
- deterministic completion without warm fallback in both Standard runs;
- safe cold fallback after a warm source-port conflict in the Extended run;
- downstream Stage 70/80/85 operation on the supplied successful runs;
- semantic Stage-90 restoration and absence of dedicated IPFW residue.

Not claimed from this evidence set:

- an `_22` run with `early_stop.triggered=true` / three Stage-60 winners;
- correction of the one observed `42003` source-port collision;
- any width greater than three;
- Model C production adoption.

## Related records

- `docs/patches/v0.4.0_7.md` — Stage-60 fixed-parent-limit corrective;
- `docs/patches/v0.4.0_8.md` — late-stage timeout containment closeout;
- `docs/verification/evidence/2026-08-10-v0.4.0_9-adaptive-validation-pass.md` — historical three-winner adaptive run;
- `docs/verification/evidence/2026-08-11-v0.4.0_21-model-b-parallel-reproducibility.md` — accepted controlled-parallel mechanism evidence;
- `docs/patches/v0.4.0_22.md` — production integration contract;
- PR #175 owner-live comment — exact supplied `_22` live summary.