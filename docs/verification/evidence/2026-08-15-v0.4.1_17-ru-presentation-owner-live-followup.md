# v0.4.1_17 — owner-live RU presentation follow-up

**Date:** 2026-08-15  
**Status:** OWNER-LIVE PARTIAL — FOLLOW-UP PATCH SELECTED

The owner installed the published `v0.4.1_17` testing package and supplied live OPNsense Diagnostics screenshots in Russian mode.

## Confirmed working presentation

The screenshots visibly confirm that the `_17` deterministic RU presentation is active for the tested screen, including:

- `Test Domain Connectivity` -> `Тестирование соединения с доменом`;
- the HTTPS guidance and `Проверка` action;
- `Blocked Domain / IP` -> `Заблокированный домен / IP`;
- `Run` -> `Запуск`;
- `Enable QUIC` -> `Включить QUIC`;
- translated result/stage presentation and `Полный профиль Стратегий Трафика`;
- ordinary circular idle output is human-readable (`Статус: ОЖИДАНИЕ`, `Состояние: ОЖИДАНИЕ`) rather than raw JSON braces.

## Remaining defects selected by the owner

The same live screenshots expose three presentation issues:

1. the content-box title still shows English `Strategy Lab`; required RU text: `Лаборатория стратегий`;
2. the Extended input row still shows English `Generic UDP (optional)`; required RU text: `UDP порт (опционально)`;
3. `Заблокированный домен / IP` wraps to two lines. The owner requires the domain input to move slightly left and the Generic UDP value control plus Enable QUIC control to use the same new left alignment.

These observations select `v0.4.1_18` as a narrow presentation/layout corrective package.

## Boundary not proven by this evidence

The screenshots do not by themselves prove Enable QUIC OFF/default persistence across a page reload/revisit. That remains a separate owner-live acceptance row.
