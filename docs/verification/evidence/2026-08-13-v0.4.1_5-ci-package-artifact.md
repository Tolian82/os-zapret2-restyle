# v0.4.1_5 — CI package build-artifact evidence

Status: **BUILD ARTIFACT READY / PERSISTENT GITHUB PACKAGE PUBLICATION PENDING / OWNER-LIVE PENDING**
Date: 2026-08-13

## Scope

This record binds the measurement-only `_5` source tree to the exact FreeBSD 15 package **build artifact** produced by CI. It proves that the package bytes were built and inspected. Under `DEC-2026-08-13-github-only-package-delivery.md`, it does **not** constitute final owner package delivery because Actions artifacts are temporary build evidence rather than the project's persistent GitHub package channel.

## Source and merge identity

- Ready PR: `#197` — `v0.4.1_5: Measure discovery probe agreement`.
- Final PR head: `a7504d95b2b5f0fe3c0b0bccea359e8f22148181`.
- Final PR-head tree: `3498b759161b14369921e9a47787e82ddbace6a2`.
- Latest-head title check: run `31645083109` / #551 — SUCCESS.
- Latest-head CI: run `31645083105` / #785 — SUCCESS.
- Corrective matrix: PASS.
- FreeBSD 15 package build, package inspection, and artifact upload: PASS.
- Exact-head squash merge: `3f85d34f415d49c2b9a3ae25bd8bdebdad2f84dd`.
- Merged-main tree: `3498b759161b14369921e9a47787e82ddbace6a2`.
- The final PR head and merged `main` therefore contain byte-identical Git trees.
- Post-merge main CI: run `31645659351` / #786 — SUCCESS; `Verify main integrity` PASS. The workflow's push policy skipped the validation/package jobs rather than rebuilding the package.

## GitHub Actions build-artifact identity

The build artifact comes from the successful FreeBSD-15 package job of latest-head CI #785, whose tested tree is exactly the merged-main tree above.

- workflow run ID: `31645083105`;
- artifact ID: `9160582106`;
- artifact name: `os-zapret2-restyle-0.4.1_5`;
- Actions artifact archive size: `410756` bytes;
- Actions artifact archive digest: `sha256:45d24f652d339a4f9bae57ba49e1e983070a92cb79288a197894ad64f61ac9e2`;
- artifact expiry reported by GitHub: `2026-11-10T22:00:32Z`.

Package inside the artifact:

- file: `os-zapret2-restyle-0.4.1_5.pkg`;
- size: `410452` bytes;
- SHA-256: `d2a8de95bb128739bcf59325433f97b6c28eb819124131c867f0f3cea9d67b4e`;
- required ABI/build contract: `FreeBSD:15:amd64`;
- FreeBSD-15 package inspection job: PASS.

## Package-delivery boundary

At the time of this build-evidence record, no persistent `_5` testing-package publication had been performed:

- no `v0.4.1_5` testing-package tag existed;
- no persistent GitHub `.pkg` package asset existed;
- no GitHub Pages update was performed;
- no pkg-repository promotion was performed.

The earlier interpretation that this Actions artifact was itself an owner-ready test package was incorrect. The active project rule is now explicit:

- Actions artifacts are intermediate build evidence;
- local/container/sandbox package copies are not final project delivery;
- an owner request for a package/testing package requires a persistent GitHub `.pkg` publication and direct GitHub URL/installation command;
- `не релиз, а пакет` forbids a stable/full project release but does not waive persistent GitHub package publication.

Therefore `_5` remains **GITHUB PACKAGE PUBLICATION PENDING** until its testing `.pkg` is persistently hosted and that publication identity is recorded separately.

Stable published package remains `_1`; latest persistently published testing package remains `_4`.

## Product/runtime boundary

`_5` remains measurement-only:

- production Stage 60 remains `C-warm-bucket-source-port-dispatch -> B-warm-worker-parallel-batched -> A-cold-fallback`;
- production discovery remains the bounded 4 KiB GET;
- `production_model_changed=false`;
- `production_discovery_policy_changed=false`;
- `production_change_recommended=false` until owner-live evidence exists.

Owner-live testing must use the persistently published `_5` testing package and measurement report. A later production change is justified only if a cheaper probe shows zero false PASS against the deep reference and a material reproducible cost improvement.
