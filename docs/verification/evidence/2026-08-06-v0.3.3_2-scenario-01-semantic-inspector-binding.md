# Live evidence — 0.3.3_2 scenario 1 semantic inspector binding failure

Status: **FAILED ATTEMPT — NOT A SCENARIO PASS**

Date: `2026-08-06`

Package under test: `os-zapret2-restyle-0.3.3_2`

System evidence:

```text
FreeBSD OPNsense.mylocal 15.1-RELEASE-p1 FreeBSD 15.1-RELEASE-p1 stable/26.7-n283674-12334a596709 SMP amd64
```

The normal Zapret2 service was running. The recorded process IDs were:

```text
CHILD_PID=92116
SUPERVISOR_PID=68205
```

The installed process wrapper was executable and correctly selected FreeBSD `ps -xww`. Both direct wrapper queries completed with exit status `0` and returned the expected commands:

```text
/usr/local/etc/zapret2/binaries/my/dvtws2 ...
/bin/sh /usr/local/opnsense/scripts/OPNsense/Zapret/supervisor_loop.sh ...
```

After sourcing `backend/common.sh`, all general process-inspection variables resolved to the wrapper:

```text
ZAPRET_PROCESS_QUERY_BIN=/usr/local/opnsense/scripts/OPNsense/Zapret/process_query.sh
COMMON_PS_BIN=/usr/local/opnsense/scripts/OPNsense/Zapret/process_query.sh
STRATEGY_LAB_SEMANTIC_PS_BIN=/usr/local/opnsense/scripts/OPNsense/Zapret/process_query.sh
```

The shared process matcher succeeded for both processes:

```text
CHILD_MATCH=true
SUPERVISOR_MATCH=true
```

However, the complete service entry point returned contradictory semantic evidence:

```json
{"schema":1,"source":"zapret_service","state":"RUNNING","child_running":false,"supervisor_running":false,"runtime_args_hash":"c5fede51792a23c60407f43d428bdc0cb564c3c0bc7a2bda3f513871889ee78d","effective_config_hash":"a6898ccbbc85229fd1abd3581b3b8bf903a45ad68ef6da9a582eb6f54de926ec","normal_firewall_hash":"ea214cdec373b9a52a796123ee24daa1fc47f22e354acd72d1b23c0121bc2a4d"}
```

Root cause confirmed in source:

- `backend/common.sh` correctly defaults semantic process inspection to `process_query.sh`;
- after loading that module, `zapret_service.sh` overwrote `STRATEGY_LAB_SEMANTIC_PS_BIN` with the direct `/bin/ps` default;
- FreeBSD daemon processes without a controlling terminal were therefore omitted only inside `strategy-lab-evidence`.

The service state, runtime-argument hash, effective-configuration hash, and normal-firewall hash were unchanged before and after the failed Strategy Lab attempt. No service stop or temporary Strategy Lab runtime mutation occurred.

Scenario 1 remains **PENDING OWNER** and must be repeated with `os-zapret2-restyle-0.3.3_4.pkg` after complete CI and FreeBSD 15 package verification.
