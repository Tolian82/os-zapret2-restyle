# v0.4.1_6 — discovery corrective live inputs

Status: **OWNER-LIVE INPUT PRESERVED / PRE-CORRECTIVE**
Date: 2026-08-13
Source package used to collect these values: `os-zapret2-restyle-0.4.1_5`

## Purpose

Preserve the final owner-supplied Stage-60 timing and non-failing discovery sample rows before Strategy Lab retention can remove the reference jobs. These values are historical input to the `_6` cleanup-finalizer correction; they are not a corrected `_6` live acceptance.

## Rutracker Model C

Reference: `job.lWLjqL`.

Persisted Stage-60 execution:

- execution model: `C-warm-bucket-source-port-dispatch`;
- stop reason: `graph_exhausted`;
- completed candidates: `16`;
- winners: `seqovl-host`, `seqovl-midsld`;
- fallbacks: none;
- Stage-60 adapter wall time: `24204 ms`.

Observed winning candidate durations:

- `seqovl-host`: `242 ms`;
- `seqovl-midsld`: `292 ms`.

The graph also contained several approximately two-second failing branches, including `golden-owner-multisplit-fake-tls-7` (`2073 ms`), `syndata-1603` (`2085 ms`) and `syndata-160301` (`2072 ms`).

### Rutracker discovery non-fail rows

Candidate `03-seqovl`:

| Variant | Classification | Reason | HTTP | Bytes | Probe ms | Total ms |
|---|---|---|---:|---:|---:|---:|
| `get-4k` | pass | `bounded_probe_passed` | 301 | 162 | 214 | 1561 |
| `deep-16k` | inconclusive | `resource_completed_short` | 301 | 162 | 206 | 1559 |
| `head` | pass | `bounded_probe_passed` | 301 | 0 | 206 | 1557 |
| `get-1` | pass | `bounded_probe_passed` | 301 | 162 | 209 | 1548 |

Every row retained `endpoint_match=true`, `intercepted=true`, `error=false`, `timeout=false`.

The deep row is intentionally inconclusive rather than FAIL because the valid selected resource completed after only 162 response bytes and therefore could not satisfy the 16-KiB body target.

## YouTube Model C

Reference: `job.NAYQxm`.

Persisted Stage-60 execution:

- execution model: `C-warm-bucket-source-port-dispatch`;
- stop reason: `enough_candidates`;
- completed candidates: `7`;
- winners: `seqovl-midsld`, `multidisorder-midsld`, `golden-owner-multisplit-fake-tls-7`;
- fallbacks: none;
- Stage-60 adapter wall time: `9151 ms`.

Winning candidate durations:

- `seqovl-midsld`: `346 ms`;
- `multidisorder-midsld`: `341 ms`;
- `golden-owner-multisplit-fake-tls-7`: `357 ms`.

This is the owner-live three-winner early-stop branch that was missing from the earlier Model-C evidence set.

### YouTube discovery non-fail rows

Candidate `golden-owner-multisplit-fake-tls-7`:

| Variant | Classification | Reason | HTTP | Bytes | Probe ms | Total ms |
|---|---|---|---:|---:|---:|---:|
| `deep-16k` | pass | `target_bytes_received` | 200 | 872109 | 275 | 1617 |
| `head` | pass | `bounded_probe_passed` | 200 | 0 | 257 | 1631 |
| `get-1` | pass | `bounded_probe_passed` | 200 | 872665 | 262 | 1604 |
| `get-4k` | pass | `bounded_probe_passed` | 200 | 874811 | 344 | 1692 |

Every row retained `endpoint_match=true`, `intercepted=true`, `error=false`, `timeout=false`.

The recorded GET-1 and GET-4K byte counts are approximately the same large response size as deep validation. The measurement therefore does not demonstrate that the requested Range reduced body transfer on this target. Saying that the server ignored Range is only an inference from these byte counts, not a separately verified HTTP fact.

## Multidomain Model-C coverage after these inputs

The owner-live set now covers three distinct Stage-60 outcomes:

1. Telegram: exhaustive 16/16 with zero winners and no fallback; retained Stage-60 duration `34209 ms`.
2. Rutracker: exhaustive 16/16 with two winners and no fallback; Stage-60 duration `24204 ms`.
3. YouTube: early stop after seven candidates with three winners and no fallback; Stage-60 duration `9151 ms`.

This strengthens Model-C runtime coverage but does not alter the `_6` scope: `_6` fixes only the measurement cleanup-boolean finalizer defect.

## Discovery-policy implication

The already preserved three-domain `_5` comparison contains 29/29 agreement for every cheap probe on comparable pairs, zero false PASS and zero false FAIL, with one Rutracker deep-inconclusive short-resource pair. Equal-sample pooled mean total times remain approximately:

- HEAD `3452.97 ms`;
- GET-1 `3442.37 ms`;
- current GET-4K `3439.70 ms`.

No material latency benefit for HEAD or GET-1 was established. Production GET-4K remains unchanged.
