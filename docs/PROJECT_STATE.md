# os-zapret2-restyle — Current state

Project: `os-zapret2-restyle`
Primary branch: `main`
Published release/package: `v0.3.2` / `os-zapret2-restyle-0.3.2_1.pkg`
Current hardening source candidate: `os-zapret2-restyle-0.3.2_35.pkg`

Patches 1–13 of the initial Strategy Lab delivery are complete. Corrective Patches 1–13 are complete in source. The second owner-approved hardening series is active.

Hardening series status:

- Patches `_25`–`_34`: required GitHub validation and FreeBSD package builds completed successfully;
- governance commit `e985323a6e0579c739f34cfbd07870405b29a848`: serial publication rule recorded and fully verified;
- Patch `_35` — unified replay-verified TLS 1.3, TLS 1.2, HTTP, QUIC, and configured UDP shortlist: current source candidate awaiting its own required GitHub checks;
- Patch `_36` and later work must not begin until `_35` is fully verified.

Accepted hardening authority:

- `docs/decisions/DEC-2026-08-05-strategy-lab-hardening-series.md`;
- `docs/decisions/DEC-2026-08-05-serial-commit-delivery.md`;
- `docs/audit/AUDIT-2026-08-05-STRATEGY-LAB-HARDENING.md`;
- `docs/architecture/STRATEGY_LAB_PROFILE_OUTPUT.md`;
- `docs/architecture/STRATEGY_LAB_UNIFIED_SHORTLIST.md`;
- revisioned records under `docs/patches/`.

Permanent decisions for this series:

- IPFW `19100–19131` is reserved exclusively for Strategy Lab and is cleaned destructively;
- automatic probes intercept only traffic generated by OPNsense itself;
- candidate PASS requires exact endpoint binding, ready temporary runtime, and positive IPFW packet-counter growth;
- every displayed profile is complete and must pass three fresh exact-profile replays;
- standard mode publishes only TLS 1.3 results;
- extended mode publishes one best verified result for each available protocol in deterministic order: TLS 1.3, TLS 1.2, HTTP, QUIC, configured UDP;
- generic UDP domain profiles use the exact replay-verified selected IPv4 addresses through `--ipset-ip=`;
- circular validation remains a TLS 1.3-only facility and consumes a separate three-to-five-item TLS 1.3 subset, never the extended-protocol display list;
- configured UDP input exposure through the supported GUI/API contract remains the next separate patch;
- GitHub publication is strictly serialized: one unverified commit at a time.

`VERSION=0.3.2`; `PLUGIN_REVISION=35`.

Next action: complete required GitHub checks and remote verification for Patch `_35`. Patch `_36` remains blocked until that succeeds.
