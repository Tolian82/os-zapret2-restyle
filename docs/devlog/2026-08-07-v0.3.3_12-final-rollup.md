# 2026-08-07 — v0.3.3_12 final package roll-up

## Purpose

Build one newest FreeBSD 15 installation candidate after the complete approved Patch 1–8 Strategy Lab third-audit corrective series has been merged to `main`.

## Scope

- keep `VERSION=0.3.3`;
- increment `PLUGIN_REVISION` from 11 to 12;
- change no Strategy Lab runtime behavior;
- preserve Patch 7 as the integrated runtime-regression qualification;
- build `os-zapret2-restyle-0.3.3_12.pkg` from the complete post-Patch-8 repository state;
- use only a FreeBSD 15 amd64 package whose manifest passes the repository package gate;
- make `_12` the owner-assisted live Scenario 1 installation candidate;
- publish no tag, testing prerelease, stable release, Pages repository, or pkg-repository package.

## Verification

The `_12` Ready PR must pass the versioned title/commit policy, complete repository validation, mandatory Strategy Lab corrective matrix, and FreeBSD 15 package build/manifest inspection on its latest head. After squash merge, the post-merge `main` run must verify integrity and, when path classification requests it, produce the final `main` package artifact.

## Live boundary

Automated success does not mark any live matrix row PASS. Scenario 1 remains `PENDING OWNER — RETEST REQUIRED`; scenarios 2–18 remain blocked until Scenario 1 passes.