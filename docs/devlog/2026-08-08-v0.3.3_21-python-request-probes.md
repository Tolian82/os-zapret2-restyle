# 2026-08-08 — v0.3.3_21 Python request/probe migration

Migration Patch 4 moves finite Strategy Lab network/request execution and parsing to Python 3.13 while preserving the Patch-3 Python stage machine and leaving candidate/family orchestration for Patch 5.

Implementation:

- added `strategy_lab_py/request.py` as the finite subprocess owner with distinct return code, stdout, stderr, timeout/termination, signal and duration evidence;
- added/refactored `strategy_lab_py/probe.py` as the stage-30/stage-40 network/baseline owner;
- changed `strategy_lab_probe_runner.sh`, `strategy_lab/request.sh`, and `extended_request.sh` into compatibility adapters;
- changed candidate IPv4 binding resolution to reuse the Python DNS ANSWER-section parser without migrating candidate runtime/family policy;
- retained the existing public network/baseline JSON and compatibility text logs, with richer sidecar evidence under each job directory;
- added focused Linux/FreeBSD regressions for parser correctness, stdout/stderr separation, command failure, timeout and public-contract parity.

The cutover exposed a pre-existing terminal publication race: result readers could observe terminal `SUCCESS` before circular eligibility had been persisted. The fix preserves all `_20` Patch-3 terminal semantics and only changes publication order so eligibility is part of the terminal snapshot before `state=completed/error` becomes visible.

Live OPNsense evidence is not advanced by this source patch. `_17` remains the latest published and owner-tested candidate, and Scenario 1 remains failed/paused until the Python migration reaches its designated parity gate.

Next migration unit after `_21` qualification: Patch 5 — candidate runtime and family screening in Python. Do not combine expansion/stability or extended-protocol orchestration with Patch 5.
