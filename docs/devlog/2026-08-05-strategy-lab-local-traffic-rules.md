# 2026-08-05 — Strategy Lab local-only automatic rules

Patch candidate: `v0.3.2_31`.

Automatic Strategy Lab candidate rules now match only traffic generated by the OPNsense host (`from me`). This applies to the standard TLS path, extended TLS/HTTP path, QUIC, and configured UDP testing, preventing the automated search from changing matching LAN-client traffic.

Circular validation remains intentionally client-wide and bidirectional because it is a separate explicit verification action.

Verification is supplied by `scripts/test-strategy-lab-local-traffic-rules.sh` and is wired into the mandatory domain-diagnostics contract suite. Patch `_32` remains blocked until GitHub validation and the FreeBSD package build for this commit complete successfully.
