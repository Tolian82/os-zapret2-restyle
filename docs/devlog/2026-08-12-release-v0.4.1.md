# Release v0.4.1 preparation

Date: 2026-08-12

Base release-preparation commit:
`ebf1d6e5519da7be1078f5688df7affde2b1e990`

==================================================
SCOPE
==================================================

Prepare the owner-authorized stable `v0.4.1` release from the published and owner-tested
`v0.4.0_26` runtime line. This cycle changes release metadata and synchronized release/
engineering documentation only; it does not introduce new Strategy Lab runtime behavior.

==================================================
RELEASE METADATA
==================================================

- `VERSION`: `0.4.1`
- `PLUGIN_REVISION`: `1`
- required release-preparation prefix: `v0.4.1_1:`
- expected immutable semantic tag: `v0.4.1`
- expected package: `os-zapret2-restyle-0.4.1_1.pkg`
- repository target: `FreeBSD:15:amd64`

==================================================
RELEASE BASIS
==================================================

- Current main before preparation is
  `ebf1d6e5519da7be1078f5688df7affde2b1e990`.
- `v0.4.0_26` passed source, corrective-matrix, FreeBSD 15 package, publication and
  owner-live gates.
- Owner-live Extended `telegram.org`, `job.xhdgCU`, proved `eligible-work-v1` production
  wiring, the exact `150/120/270/120` budget on the measured topology, Model-C 16/16
  graph exhaustion without fallback, and clean Stage-90 restoration.
- The preceding accepted evidence retains the Model-C working-candidate path, controlled
  source-port lease correction and Model-B fallback/reference baseline.
- The active risk-based release policy does not convert unrelated pending regression rows
  into blanket stable-release blockers when the release-preparation change itself adds no
  new runtime behavior.

==================================================
RELEASE PROTOCOL
==================================================

Pull-request title, branch-commit prefix and required squash subject:

`v0.4.1_1: Prepare release v0.4.1`

After the final release-preparation head passes the complete applicable PR check set,
squash merge it with the exact subject above. Because `VERSION` changes on `main`,
`release-trigger.yml` must create/verify immutable tag `v0.4.1` at that merge and dispatch
`release.yml`.

The release cycle is complete only after exact tag placement, release workflow, FreeBSD 15
package `os-zapret2-restyle-0.4.1_1.pkg`, checksum, GitHub Release and Pages/pkg repository
publication are verified.
