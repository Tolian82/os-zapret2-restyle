# Release v0.4.0 preparation

Date: 2026-08-09

Base release-preparation commit:
`2827ce943912ce77659a138823601a4fca869ef3`

==================================================
SCOPE
==================================================

Prepare the owner-authorized stable `v0.4.0` release from the current `_27` source and
its recorded Scenario-1 live PASS. This cycle changes release metadata and synchronized
documentation only; it does not add runtime behavior.

==================================================
RELEASE METADATA
==================================================

- `VERSION`: `0.4.0`
- `PLUGIN_REVISION`: `1`
- expected immutable tag: `v0.4.0`
- expected package: `os-zapret2-restyle-0.4.0_1.pkg`
- repository target: `FreeBSD:15:amd64`

==================================================
RELEASE BASIS
==================================================

- Strategy Lab source, corrective series and Python migration are merged.
- Corrective `_27` passed the complete source/CI and FreeBSD 15 package gates.
- Owner live Standard Scenario 1 passed on `_27`, including exact Stage-90 restoration.
- The active risk-based release policy selects that Scenario 1 as v0.4.0's mandatory
  live row; rows 2–18 remain regression inventory without false PASS claims.
- The adaptive-search `_28`–`_33` series is deliberately outside this release.

==================================================
RELEASE PROTOCOL
==================================================

Pull-request title, commit subject and required squash subject:

`v0.4.0_1: Prepare release v0.4.0`

After the unchanged release-preparation head passes its complete PR check set, squash
merge it with the exact subject above. `release-trigger.yml` must then create/verify tag
`v0.4.0` at the merge and dispatch `release.yml`.

The release cycle is complete only after tag placement, release workflow, package,
checksum and Pages/pkg repository publication are verified.

==================================================
COMPLETION
==================================================

Completed on 2026-08-09:

- release-preparation PR/CI and FreeBSD 15 package qualification passed;
- immutable tag `v0.4.0` resolves to release commit
  `5e2f98c503a94413be76d7fd6b7f5721fc436f56`;
- Release workflow run 17 completed successfully;
- `os-zapret2-restyle-0.4.0_1.pkg` and `SHA256SUMS` were published;
- the matching `FreeBSD:15:amd64` Pages/pkg repository was published;
- post-release EOF whitespace was repaired without changing release metadata/runtime,
  and current `main` push CI run 564 passed;
- the owner installed `0.4.0_1` on OPNsense.

Release closure does not mark pending live-matrix rows as PASS. The next source cycle is
the separately approved adaptive-search `_28` change.
