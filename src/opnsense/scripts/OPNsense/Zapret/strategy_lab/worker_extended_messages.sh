case "${LANGUAGE}" in
    ru) PARTIAL_FINAL_MESSAGE='Strategy Lab: TLS 1.3 shortlist и расширенные TLS 1.2/HTTP результаты готовы; QUIC и произвольный UDP пока не проверяются.' ;;
    *) PARTIAL_FINAL_MESSAGE='Strategy Lab has TLS 1.3 shortlist and extended TLS 1.2/HTTP results; QUIC and arbitrary UDP are not active yet.' ;;
esac
