case "${LANGUAGE}" in
    ru) PARTIAL_FINAL_MESSAGE='Strategy Lab: TLS 1.3 shortlist, TLS 1.2/HTTP и QUIC результаты готовы; произвольный UDP пока не проверяется.' ;;
    *) PARTIAL_FINAL_MESSAGE='Strategy Lab has TLS 1.3 shortlist, TLS 1.2/HTTP, and QUIC results; arbitrary UDP is not active yet.' ;;
esac
