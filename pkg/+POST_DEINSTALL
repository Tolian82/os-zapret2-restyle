#!/bin/sh

# Post-deinstall hook for os-zapret2-restyle.
# Reloads configd after plugin action definitions are removed.

if [ -x /usr/local/etc/rc.d/configd ]; then
    /usr/local/etc/rc.d/configd restart >/dev/null 2>&1 || true
fi

exit 0
